openresty's luajit has some extended functions which functional tests does not expect to find.

diff --git a/test/functional/core/main_spec.lua b/test/functional/core/main_spec.lua
index 90183ef..30150c0 100644
--- a/test/functional/core/main_spec.lua
+++ b/test/functional/core/main_spec.lua
@@ -246,7 +246,13 @@ describe('vim._core', function()
         'jit.util',
         'string.buffer',
         'table.clear',
+        'table.clone',
+        'table.isarray',
+        'table.isempty',
         'table.new',
+        'table.nkeys',
+        'thread.exdata',
+        'thread.exdata2',
       }, expected)
     end
     t.eq(expected, n.exec_lua([[local t = vim.tbl_keys(package.preload); table.sort(t); return t]]))
