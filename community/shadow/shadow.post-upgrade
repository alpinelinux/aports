#!/bin/sh

ver_old=$2

if [ "$(apk version -t "$ver_old" '4.10-r0')" = '<' ]; then
	cat >&2 <<-EOF
	*
	* Utilities pwconv, pwunconv, grpconv, and grpunconv have been moved
	* to a subpackage. If you use them, run: apk add shadow-conv.
	* 
	EOF
fi

exit 0
