https://github.com/bats-core/bats-assert/pull/84#discussion_r2423133406

diff --git a/src/assert_line.bash b/src/assert_line.bash
index 35df5b8..0e9cf5b 100644
--- a/src/assert_line.bash
+++ b/src/assert_line.bash
@@ -217,7 +217,7 @@ Did you mean to call \`assert_line\` or \`assert_stderr_line\`?" \
   # Arguments.
   local -r expected="$1"
 
-  if (( is_mode_regexp == 1 )) then
+  if (( is_mode_regexp == 1 )); then
     __check_is_valid_regex "$expected" "$caller" || return 1
   fi
 
