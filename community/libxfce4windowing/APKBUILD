# Maintainer: Natanael Copa <ncopa@alpinelinux.org>
pkgname=libxfce4windowing
pkgver=4.20.6
pkgrel=0
pkgdesc="Windowing concept abstraction library for X11 and Wayland"
url="https://gitlab.xfce.org/xfce/libxfce4windowing"
arch="all"
license="LGPL-2.1-or-later"
makedepends="
	glib-dev
	gobject-introspection-dev
	gtk+3.0-dev
	libdisplay-info-dev
	libwnck3-dev
	libx11-dev
	libxrandr-dev
	meson
	ninja
	vala
	wayland-dev
	wlr-protocols
	xfce4-dev-tools
	"
subpackages="$pkgname-dev $pkgname-lang"
source="https://gitlab.xfce.org/xfce/libxfce4windowing/-/archive/libxfce4windowing-$pkgver/libxfce4windowing-libxfce4windowing-$pkgver.tar.bz2"
builddir="$srcdir/libxfce4windowing-libxfce4windowing-$pkgver"

build() {
	abuild-meson \
		-Dvala=enabled \
		output .
	meson compile -C output
}

check() {
	meson test --print-errorlogs -C output
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

sha512sums="
b141b09fd542f61b43e6242bcb1d51ad61b00decac08875d89eacca47987953310078246511222fe94b57121cc21d4bbedaa0bf7c00e5c9410b2cfb1624a0f52  libxfce4windowing-libxfce4windowing-4.20.6.tar.bz2
"
