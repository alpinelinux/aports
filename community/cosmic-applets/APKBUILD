# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-applets
pkgver=1.0.10
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Applets for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-applets"
# 32-bit: https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/issues/111
# s390x: nix
# riscv64: too slow to build
arch="all !armhf !armv7 !x86 !s390x !riscv64"
license="GPL-3.0-only"
depends="
	cosmic-icons
"
makedepends="
	cargo
	clang-libclang
	dbus-dev
	findutils
	just
	libinput-dev
	libxkbcommon-dev
	pipewire-dev
	pulseaudio-dev
"
subpackages="$pkgname-systemd"
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-applets/archive/refs/tags/epoch-$_upstreamver.tar.gz"
options="net !check"  # fetch dependencies, no tests
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	just build-release --frozen
}

check() {
	cargo test --release --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
c9c3886bce5b3a260a9823534e44ed0b1f99ca19f3f2055f464b5437ebdfae286529ce4c45db8f792be93213fe0dd4d8f0fa822c0bd70665bbd17f2a09c9d106  cosmic-applets-1.0.10.tar.gz
"
