diff --git a/src/webdav.c b/src/webdav.c
index a7c4ce9..dcf1bdd 100644
--- a/src/webdav.c
+++ b/src/webdav.c
@@ -2041,7 +2041,7 @@ ssl_verify(void *userdata, int failures, const ne_ssl_certificate *cert)
             len = getline(&s, &n, stdin);
             if (len < 0)
                 abort();
-            if (rpmatch(s) > 0)
+            if ((s[0]=='y' || s[0]=='Y') > 0)
                 ret = 0;
             free(s);
     }
