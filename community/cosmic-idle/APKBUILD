# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-idle
pkgver=1.6.0
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Idle daemon for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-idle"
# riscv64: too slow to build
arch="all !riscv64"
license="GPL-3.0-only"
depends="
	cosmic-icons
"
makedepends="
	cargo-auditable
	just
	libxkbcommon-dev
	wayland-dev
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-idle/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CHOST" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
c34b127d1f3b82be44a9d91f8f8dcd7a46aec9b44d3acdb06dcf0cdd00cf2aa5f5674809ee03b4c75ea8f99ce781c1e525d32711d4703f995c61ecf86d5ed0a0  cosmic-idle-1.6.0.tar.gz
"
