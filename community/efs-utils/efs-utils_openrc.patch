--- a/src/efs_utils_common/proxy.py
+++ b/src/efs_utils_common/proxy.py
@@ -603,6 +603,10 @@
             logging.warning("Unable to read %s", comm_file)
     else:
         init_system = "launchd"
+
+    if init_system == "init" and os.path.exists("/run/openrc/softlevel"):
+        init_system = "openrc"
+        logging.debug("Detected OpenRC init system")
 
     logging.debug("Identified init system: %s", init_system)
     return init_system
@@ -625,6 +629,20 @@
                 close_fds=True,
             )
         elif "start" in str(status):
+            logging.debug("%s is already running", WATCHDOG_SERVICE)
+
+    elif init_system == "openrc":
+        rc = subprocess.call(
+            ["/sbin/rc-service", WATCHDOG_SERVICE, "status"], close_fds=True
+        )
+        if rc != 0:
+            subprocess.Popen(
+                ["/sbin/rc-service", WATCHDOG_SERVICE, "start"],
+                stdout=subprocess.DEVNULL,
+                stderr=subprocess.DEVNULL,
+                close_fds=True,
+            )
+        else:
             logging.debug("%s is already running", WATCHDOG_SERVICE)
 
     elif init_system == "systemd":
