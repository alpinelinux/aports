Minimize size of the resulting binary.

--- a/Cargo.toml
+++ b/Cargo.toml
@@ -111,6 +111,8 @@
 [profile.release]
 codegen-units = 1
 lto = true
+opt-level = "s"
+panic = "abort"
 
 [[bin]]
 name = "starship"
