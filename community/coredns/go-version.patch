--- a/Makefile
+++ b/Makefile
@@ -7,10 +7,8 @@ BUILDOPTS?=-v
 GOPATH?=$(HOME)/go
 MAKEPWD:=$(dir $(realpath $(firstword $(MAKEFILE_LIST))))
 CGO_ENABLED?=0
-GOLANG_VERSION ?= $(shell cat .go-version)

 export GOSUMDB = sum.golang.org
-export GOTOOLCHAIN = go$(GOLANG_VERSION)

 .PHONY: all
 all: coredns
