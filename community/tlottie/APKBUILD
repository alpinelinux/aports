# Maintainer: Milan P. Stanić <mps@arvanta.net>
pkgname=tlottie
pkgver=0.0_git202609081
pkgrel=0
_commit=4b940c7942fbde8ee56f10f39a5224a4153bd91e
pkgdesc="rust library for drawing lottie animations"
url="https://github.com/dkaraush/tlottie"
arch="all"
license="MIT"
options="!check" # one test fail
makedepends="cargo cargo-auditable"
subpackages="$pkgname-dev"
source="$pkgname-$pkgver.tar.gz::https://github.com/dkaraush/tlottie/archive/$_commit.tar.gz"
builddir="$srcdir/"$pkgname-$_commit

prepare() {
	default_prepare

	cargo fetch --target="$CHOST" --locked
}

build() {
	cargo auditable build --frozen --release
}

check() {
	cargo test --frozen
}

package() {
	install -vDm755 -t "$pkgdir/usr/lib" target/release/libtlottie.so
	install -vDm644 -t "$pkgdir/usr/include" include/tlottie.h
}

sha512sums="
ffff56251fe85b5736e13c4ad6c82b0217e4ed3a2bf5f62d8ebc1ff6771b2fcd35d22845cdcbb23d84dcac13b20ff2ebc89b8451a0fa42aa8827f3db312316ca  tlottie-0.0_git202609081.tar.gz
"
