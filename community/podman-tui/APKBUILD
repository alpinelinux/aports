# Contributor: omni <omni+alpine@hack.org>
# Maintainer: omni <omni+alpine@hack.org>
pkgname=podman-tui
pkgver=0.13.0
pkgrel=0
pkgdesc="Terminal user interface for Podman v4"
url="https://github.com/containers/podman-tui"
license="Apache-2.0"
# podman (netavark & aardvark-dns -> rust/cargo)
arch="all !s390x"
makedepends="go gpgme-dev linux-headers btrfs-progs-dev lvm2-dev"
depends="podman"
options="!check"
source="$pkgname-$pkgver.tar.gz::https://github.com/containers/podman-tui/archive/v$pkgver.tar.gz"

export GOFLAGS="$GOFLAGS -trimpath -mod=readonly -modcacherw"
export GOCACHE="${GOCACHE:-"$srcdir/go-cache"}"
export GOTMPDIR="${GOTMPDIR:-"$srcdir"}"
export GOMODCACHE="${GOMODCACHE:-"$srcdir/go"}"

build() {
	# https://github.com/mattn/go-sqlite3/issues/1164
	export CGO_CFLAGS="$CFLAGS -D_LARGEFILE64_SOURCE"

	go build -v .
}

package() {
	install -Dm0755 "$pkgname" -t "$pkgdir"/usr/bin
}

sha512sums="
cf7fcf540c159910303cb23eb8d7370bc89cf48a9068ee1a955ebe2f29ab23e237a9d832afe5a4b0d19d23c4f8604fb4463b2964093c682183718d34b9632c87  podman-tui-0.13.0.tar.gz
"
