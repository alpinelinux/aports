# Contributor: Rasmus Thomsen <oss@cogitri.dev>
# Maintainer: Natanael Copa <ncopa@alpinelinux.org>
pkgname=fractal
pkgver=12.1
pkgrel=0
pkgdesc="Matrix group messaging app"
url="https://gitlab.gnome.org/World/fractal"
# 32 bits OOM and s390x blocked by glycin-loaders
arch="all !armv7 !armhf !x86 !s390x"
license="GPL-3.0-or-later"
depends="
	dbus:org.freedesktop.Secrets
	glycin-loaders
	xdg-utils
	gst-plugins-rs-gtk4
	"
makedepends="
	cargo
	clang-libclang
	cmake
	dbus-dev
	desktop-file-utils
	glib-dev
	gmp-dev
	grass
	gspell-dev
	gst-editing-services-dev
	gst-plugins-bad-dev
	gst-plugins-base-dev
	gstreamer-dev
	gtk4.0-dev
	gtksourceview5-dev
	lcms2-dev
	libadwaita-dev
	libseccomp-dev
	libshumate-dev
	meson
	openssl-dev
	pipewire-dev
	xdg-desktop-portal-dev
	"
options="!check" # no tests
subpackages="$pkgname-lang"
source="$pkgname-$pkgver.tar.gz::https://gitlab.gnome.org/GNOME/fractal/-/archive/$pkgver/fractal-$pkgver.tar.gz"

case "$CARCH" in
	x86)
		export CFLAGS="$CFLAGS -fno-stack-protector"
		;;
esac


build() {
	abuild-meson . output
	meson compile -C output
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

sha512sums="
93739acdeabbf4ec6c3d9abd2fb4ee2ec7f01faf69aa58c73c89ed7fd801cdd308b623a7e9bde1106df77c95cbb8c9b6461bfd5fdc7ef4bc4e2fdad48490ac49  fractal-12.1.tar.gz
"
