unifont.ttf is not available on alpine.

--- a/mkfont.sh
+++ b/mkfont.sh
@@ -15,7 +15,7 @@ ASCII_RANGE="0x0-0x7f,${UNICODE_ARROWS},${UNICODE_LINES}"
 
 mk="grub-mkfont -v"
 
-input="/usr/share/fonts/truetype/unifont/unifont.ttf"
+input="/usr/share/fonts/opentype/unifont/unifont.otf"
 
 # Since we don't have localization we only need ASCII here (0x0-0x7f)
 ## The action description box font
