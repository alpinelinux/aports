Needed for CARGO_NET_OFFLINE=true

--- a/meson.build
+++ b/meson.build
@@ -39,7 +39,6 @@ else
 endif
 
 cargo_env = environment()
-cargo_env.set('CARGO_HOME', build_root / 'cargo-home')
 
 # If the typename is internal, then we have built apk
 # from the subproject and we nee to tell cargo to look into that
