# Configuration for /etc/init.d/ipfixcol2

# Path to the startup configuration file.
# See /usr/share/doc/ipfixcol2 provided by ipfixcol2-doc package for examples.
#cfgfile="/etc/ipfixcol2/startup.xml"

# User and group to run ipfixcol2 as.
#command_user="ipfixcol:ipfixcol"

# Additional options to pass to ipfixcol2.
command_args="-v"

# Run with process supervisor.
supervisor=supervise-daemon
