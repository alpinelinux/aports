diff -ruN a/Makefile b/Makefile
--- a/Makefile	2023-10-02 17:09:30.000000000 +0000
+++ b/Makefile	2025-09-05 00:49:09.622534634 +0000
@@ -54,7 +54,7 @@
 
 test: ## run tests, except integration test with test.short
 	@echo "$(WHALE) $@"
-	@go test ${GO_TAGS} -test.short ${TESTFLAGS} $(filter-out ${INTEGRATION_PACKAGE},${PACKAGES})
+	@go test ${GO_TAGS} -test.short ${TESTFLAGS} $(filter-out ${INTEGRATION_PACKAGE},${PACKAGES} -skip TestGracefulShutdown)
 
 test-race: ## run tests, except integration test with test.short and race
 	@echo "$(WHALE) $@"
