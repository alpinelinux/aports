# Contributor: Bart Ribbers <bribbers@disroot.org>
# Contributor: Celeste <cielesti@protonmail.com>
maintainer="Achill Gilgenast <achill@achill.org>"
pkgname=catch2-3
pkgver=3.13.0
pkgrel=0
pkgdesc="Modern, C++-native, header-only, test framework for unit-tests (v3)"
url="https://github.com/catchorg/Catch2"
arch="all"
license="BSL-1.0"
makedepends="
	cmake
	python3
	samurai
	"
subpackages="$pkgname-doc"
source="https://github.com/catchorg/Catch2/archive/v$pkgver/catch2-$pkgver.tar.gz"
builddir="$srcdir/Catch2-$pkgver"

build() {
	# Set -DCATCH_DEVELOPMENT_BUILD to enable tests
	cmake -B build -G Ninja \
		-DCMAKE_BUILD_TYPE=MinSizeRel \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_INSTALL_LIBDIR=lib \
		-DCATCH_DEVELOPMENT_BUILD="$(want_check && echo ON || echo OFF)" \
		-DCATCH_BUILD_EXTRA_TESTS="$(want_check && echo ON || echo OFF)" \
		-DCATCH_ENABLE_WERROR=OFF
	cmake --build build
}

check() {
	# ApprovalTests is broken https://github.com/catchorg/Catch2/issues/1780
	ctest --test-dir build -E "ApprovalTests"
}

package() {
	DESTDIR="$pkgdir" cmake --install build
}

sha512sums="
7eea385d79d88a5690cde131fe7ccda97d5c54ea09d6f515000d7bf07c828809d61c1ac99912c1ee507cf933f61c1c47ecdcc45df7850ffa82714034b0fccf35  catch2-3.13.0.tar.gz
"
