# Contributor: Kevin Wang <kevin@muxable.com>
# Maintainer: Kevin Wang <kevin@muxable.com>
pkgname=librist
pkgver=0.2.15
pkgrel=0
pkgdesc="Reliable Internet Stream Transport (RIST)"
url="https://www.rist.tv/"
arch="all"
license="BSD-2-Clause"
makedepends="meson linux-headers cjson-dev mbedtls3-dev"
checkdepends="cmocka-dev"
subpackages="$pkgname-progs $pkgname-dev"
source="$pkgname-$pkgver-1.tar.gz::https://code.videolan.org/rist/librist/-/archive/v$pkgver/librist-v$pkgver.tar.gz"
builddir="$srcdir/librist-v$pkgver"

build() {
	abuild-meson build
	meson compile -C build
}

check() {
	# Tests are flaky & will fail if packets do not arrive in exact order.
	# This often leads to having to retry the whole CI pipeline for s390x.
	# Hopefully, repeating tests 3 times before failing will prevent that.
	local i; for i in $(seq 0 3); do
		[ $i -eq 0 ] || msg "Retrying ($i/3)..."
		meson test --print-errorlogs --num-processes 1 \
			--no-suite multicast -C build && return 0
		sleep 1
	done
	return 1
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C build
}

progs() {
	pkgdesc="Tools for working with RIST"
	depends=""
	amove usr/bin
}

sha512sums="
906382576025b39b9ac13e4ad20648b5c2a61e215d7dc0d30e043dc3c30d6fd9586c7349cfeb2c5fd4c576e833c16dbe003c6f774a6372ea1bfc3c4fefa81663  librist-0.2.15-1.tar.gz
"
