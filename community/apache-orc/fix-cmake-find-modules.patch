diff --git a/c++/orcConfig.cmake.in b/c++/orcConfig.cmake.in
index c794822..d7542ef 100644
--- a/c++/orcConfig.cmake.in
+++ b/c++/orcConfig.cmake.in
@@ -40,7 +40,7 @@ if(DEFINED CMAKE_MODULE_PATH)
 else()
   unset(ORC_CMAKE_MODULE_PATH_OLD)
 endif()
-set(CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
+list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
 
 include(CMakeFindDependencyMacro)
 foreach(dependency ${ORC_SYSTEM_DEPENDENCIES})
diff --git a/cmake_modules/FindGTestAlt.cmake b/cmake_modules/FindGTestAlt.cmake
index 802a88d..ef1c48a 100644
--- a/cmake_modules/FindGTestAlt.cmake
+++ b/cmake_modules/FindGTestAlt.cmake
@@ -32,7 +32,6 @@ file (TO_CMAKE_PATH "${GTEST_HOME}" _gtest_path )
 
 find_path (GTEST_INCLUDE_DIR gmock/gmock.h HINTS
   ${_gtest_path}
-  NO_DEFAULT_PATH
   PATH_SUFFIXES "include")
 
 find_library (GMOCK_LIBRARY NAMES gmock HINTS
diff --git a/cmake_modules/FindLZ4Alt.cmake b/cmake_modules/FindLZ4Alt.cmake
index dc59983..86f711d 100644
--- a/cmake_modules/FindLZ4Alt.cmake
+++ b/cmake_modules/FindLZ4Alt.cmake
@@ -40,7 +40,6 @@ message (STATUS "LZ4_HOME: ${LZ4_HOME}")
 
 find_path (LZ4_INCLUDE_DIR lz4.h HINTS
   ${_lz4_path}
-  NO_DEFAULT_PATH
   PATH_SUFFIXES "include")
 
 find_library (LZ4_LIBRARY NAMES lz4 liblz4 HINTS
diff --git a/cmake_modules/FindProtobufAlt.cmake b/cmake_modules/FindProtobufAlt.cmake
index efc0041..94edf7d 100644
--- a/cmake_modules/FindProtobufAlt.cmake
+++ b/cmake_modules/FindProtobufAlt.cmake
@@ -96,12 +96,10 @@ if (ProtobufAlt_FOUND)
 else()
     find_path (PROTOBUF_INCLUDE_DIR google/protobuf/io/zero_copy_stream.h HINTS
       ${_protobuf_path}
-      NO_DEFAULT_PATH
       PATH_SUFFIXES "include")
 
     find_path (PROTOBUF_INCLUDE_DIR google/protobuf/io/coded_stream.h HINTS
       ${_protobuf_path}
-      NO_DEFAULT_PATH
       PATH_SUFFIXES "include")
 
     find_library (PROTOBUF_LIBRARY NAMES protobuf libprotobuf HINTS
@@ -122,7 +120,6 @@ else()
 
     find_program(PROTOBUF_EXECUTABLE protoc HINTS
       ${_protobuf_path}
-      NO_DEFAULT_PATH
       PATH_SUFFIXES "bin")
 endif ()
 
diff --git a/cmake_modules/FindSnappyAlt.cmake b/cmake_modules/FindSnappyAlt.cmake
index e9a5eb7..5c41e1d 100644
--- a/cmake_modules/FindSnappyAlt.cmake
+++ b/cmake_modules/FindSnappyAlt.cmake
@@ -44,7 +44,6 @@ message (STATUS "SNAPPY_HOME: ${SNAPPY_HOME}")
 
 find_path (SNAPPY_INCLUDE_DIR snappy.h HINTS
   ${_snappy_path}
-  NO_DEFAULT_PATH
   PATH_SUFFIXES "include")
 
 find_library (SNAPPY_LIBRARY NAMES snappy HINTS
diff --git a/cmake_modules/FindZLIBAlt.cmake b/cmake_modules/FindZLIBAlt.cmake
index 9623271..eda1edb 100644
--- a/cmake_modules/FindZLIBAlt.cmake
+++ b/cmake_modules/FindZLIBAlt.cmake
@@ -40,7 +40,6 @@ message (STATUS "ZLIB_HOME: ${ZLIB_HOME}")
 
 find_path (ZLIB_INCLUDE_DIR zlib.h HINTS
   ${_zlib_path}
-  NO_DEFAULT_PATH
   PATH_SUFFIXES "include")
 
 if (NOT ZLIB_STATIC_LIB_NAME)
diff --git a/cmake_modules/FindZSTDAlt.cmake b/cmake_modules/FindZSTDAlt.cmake
index 402f705..8283465 100644
--- a/cmake_modules/FindZSTDAlt.cmake
+++ b/cmake_modules/FindZSTDAlt.cmake
@@ -40,7 +40,6 @@ message (STATUS "ZSTD_HOME: ${ZSTD_HOME}")
 
 find_path (ZSTD_INCLUDE_DIR zstd.h HINTS
         ${_zstd_path}
-        NO_DEFAULT_PATH
         PATH_SUFFIXES "include")
 
 find_library (ZSTD_LIBRARY NAMES zstd HINTS
