Without setting `vsn` from "$pkgver", it is detected as "0.0.0".

--- a/apps/els_lsp/src/els_lsp.app.src
+++ b/apps/els_lsp/src/els_lsp.app.src
@@ -1,6 +1,6 @@
 {application, els_lsp, [
     {description, "Erlang LS - Language Server Protocol"},
-    {vsn, git},
+    {vsn, "@ERLANG_LS_VERSION@"},
     {registered, []},
     {mod, {els_app, []}},
     {applications, [
