diff -rupN a/setup.py b/setup.py
--- a/setup.py	2025-05-09 15:00:04.000000000 +0000
+++ b/setup.py	2026-03-13 22:24:24.750000000 +0000
@@ -15,11 +15,17 @@ See also:
 from setuptools import setup
 
 
-# (1) check required versions (from https://medium.com/@daveshawley/safely-using-setup-cfg-for-metadata-1babbe54c108)
-import pkg_resources
+# (1) check required versions
+from importlib.metadata import version, PackageNotFoundError
 
-pkg_resources.require("setuptools>=39.2")
-pkg_resources.require("setuptools_scm")
+setuptools_version = version("setuptools")
+setuptools_scm_version = version("setuptools_scm")
+
+setuptools_version_min = 39.2
+setuptools_version_minor = float(setuptools_version.split(".")[0]) + \
+    float(setuptools_version.split(".")[1])
+if setuptools_version_minor < setuptools_version_min:
+    raise PackageNotFoundError("setuptools>=" + str(setuptools_version_min))
 
 
 # (2) Generate download url using git version
@@ -33,8 +39,6 @@ DOWNLOAD_URL = URL + "/tarball/" + get_v
 args = {
     "write_to": "src/makefun/_version.py",
 }
-# Use the 'version_file_template' directive if possible to avoid type hints and annotations (python <3.8)
-setuptools_scm_version = pkg_resources.get_distribution("setuptools_scm").version
 # for some reason importing packaging.version.Version here fails on python 3.5
 # from packaging.version import Version
 # if Version(setuptools_scm_version) >= Version('6'):
