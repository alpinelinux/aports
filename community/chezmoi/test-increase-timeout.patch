On loongarch64 and riscv64, the internal/cmd tests timeout. Increase the timeout
to give those arches a chance to pass.

diff --git a/Makefile b/Makefile
index f6c3f4f..1c12424 100644
--- a/Makefile
+++ b/Makefile
@@ -85,8 +85,8 @@ rm-dist:
 
 .PHONY: test
 test:
-	${GO} test -ldflags="-X chezmoi.io/chezmoi/internal/chezmoitest.umaskStr=0o022" ./...
-	${GO} test -ldflags="-X chezmoi.io/chezmoi/internal/chezmoitest.umaskStr=0o002" ./...
+	${GO} test -timeout 30m -ldflags="-X chezmoi.io/chezmoi/internal/chezmoitest.umaskStr=0o022" ./...
+	${GO} test -timeout 30m -ldflags="-X chezmoi.io/chezmoi/internal/chezmoitest.umaskStr=0o002" ./...
 
 .PHONY: test-docker
 test-docker:
