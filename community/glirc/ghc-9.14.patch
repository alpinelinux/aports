diff --git a/glirc.cabal b/glirc.cabal
index b034afa6..5fd24816 100644
--- a/glirc.cabal
+++ b/glirc.cabal
@@ -22,13 +22,15 @@ bug-reports:         https://github.com/glguy/irc-core/issues
 tested-with:         GHC == 9.0.2, GHC == 9.4.8, GHC == 9.8.2
 
 custom-setup
-  setup-depends: base     >=4.12 && <4.21,
+  setup-depends: base     >=4.12 && <4.23,
                  filepath >=1.4  && <1.6,
-                 Cabal    >=2.2  && <4
+                 Cabal    >=2.2  && <4,
+                 bytestring,
+                 containers,
 
 source-repository head
   type: git
-  location: git://github.com/glguy/irc-core.git
+  location: https://github.com/glguy/irc-core.git
   branch: v2
 
 executable glirc
@@ -42,7 +44,7 @@ executable glirc
   -- Constraints can be found on the library itself
   build-depends:
     base, glirc, lens, text, vty,
-    vty-unix             ^>=0.2.0.0,
+    vty-unix             ^>=0.3.0.0,
 
   if os(Linux)
     ld-options: -Wl,--dynamic-list=exec/linux_exported_symbols.txt
@@ -169,7 +171,7 @@ library
     Build_glirc
 
   build-depends:
-    base                 >=4.15   && <4.21,
+    base                 >=4.15   && <4.23,
     HsOpenSSL            >=0.11   && <0.12,
     async                >=2.2    && <2.3,
     attoparsec           ^>=0.14,
@@ -177,7 +179,7 @@ library
     bytestring           >=0.10.8 && <0.13,
     config-schema        ^>=1.3.0.0,
     config-value         ^>=0.8,
-    containers           >=0.5.7  && <0.8,
+    containers           >=0.5.7  && <0.9,
     curve25519           ^>=0.2.5,
     directory            >=1.2.6  && <1.4,
     filepath             >=1.4.1  && <1.6,
@@ -188,7 +190,7 @@ library
     irc-core             ^>=2.13,
     kan-extensions       >=5.0    && <5.3,
     lens                 >=4.14   && <5.4,
-    random               >=1.1    && <1.3,
+    random               >=1.1    && <1.4,
     network              >=2.6.2  && <3.3,
     typed-process        ^>=0.2.10,
     psqueues             >=0.2.7  && <0.3,
@@ -196,14 +198,14 @@ library
     semigroupoids        >=5.1    && <6.1,
     split                >=0.2    && <0.3,
     stm                  >=2.4    && <2.6,
-    template-haskell     >=2.11   && <2.23,
+    template-haskell     >=2.11   && <2.25,
     text                 >=1.2.2  && <2.2,
-    time                 >=1.9    && <1.15,
+    time                 >=1.9    && <1.17,
     transformers         >=0.5.2  && <0.7,
     unix                 >=2.7    && <2.9,
     unordered-containers >=0.2.11 && <0.3,
     vector               >=0.11   && <0.14,
-    vty                  ^>=6.2,
+    vty                  >=6.4,
 
 test-suite test
   type:                exitcode-stdio-1.0
