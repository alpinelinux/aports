# Contributor: Rasmus Thomsen <oss@cogitri.dev>
# Maintainer: team/gnome <achill@achill.org>
pkgname=template-glib
pkgver=3.38.0
pkgrel=0
# NOTE: Upgrade only to even-numbered versions (e.g. 1.18.x, 1.20.x)!
pkgdesc="library for template expansion which supports calling into GObject Introspection from templates"
url="https://gitlab.gnome.org/GNOME/template-glib"
arch="all"
license="GPL-3.0-or-later"
makedepends="
	bison
	flex
	glib-dev
	gobject-introspection-dev
	meson
	vala
	"
subpackages="$pkgname-dev $pkgname-lang"
source="https://download.gnome.org/sources/template-glib/${pkgver%.*}/template-glib-$pkgver.tar.xz"

build() {
	abuild-meson \
		-Db_lto=true \
		. output
	meson compile -C output
}

check() {
	meson test --print-errorlogs -C output
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

sha512sums="
ede736ed4eb629a96d21a93d4d2a5df2b2b4efd1ea239e10862fd8058f26cc4ed342ae33a115e05ab831586f1df99f16ebf8c815ab1725fc5b0a08d94c566b26  template-glib-3.38.0.tar.xz
"
