# Contributor: Siva Mahadevan <me@svmhdvn.name>
# Maintainer: Willow Barraco <contact@willowbarraco.fr>
pkgname=kakoune-lsp
pkgver=20.0.0
pkgrel=0
pkgdesc="Kakoune Language Server Protocol Client"
url="https://github.com/kakoune-lsp/kakoune-lsp"
arch="all"
license="Unlicense OR MIT"
makedepends="cargo cargo-auditable"
options="net" # cargo
source="$pkgname-$pkgver.tar.gz::https://github.com/kakoune-lsp/kakoune-lsp/archive/refs/tags/v$pkgver.tar.gz"

prepare() {
	default_prepare

	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo auditable build --frozen --release
}

check() {
	cargo test --frozen --release
}

package() {
	install -Dvm755 target/release/kak-lsp -t "$pkgdir"/usr/bin/
}

sha512sums="
cbea29419e1c54e398a31e5dce81786092bec4005952e1a04eea1333374d9e0e420e719b61bccb2f3644efb04bbe13a761a1549fd76b6d977e00a2f4cf72e3ff  kakoune-lsp-20.0.0.tar.gz
"
