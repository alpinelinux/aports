diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 09fc3796..b6290bdd 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -140,3 +140,14 @@ install(EXPORT mlocale6Targets
     NAMESPACE mlocale6::
     DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/mlocale6
 )
+
+set(target mlocale6)
+configure_file(
+    ${PROJECT_NAME}.pc.in
+    ${PROJECT_NAME}.pc @ONLY
+)
+
+install(
+    FILES ${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}.pc
+    DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig
+)
diff --git a/src/mlocale6.pc.in b/src/mlocale6.pc.in
new file mode 100644
index 00000000..884bc423
--- /dev/null
+++ b/src/mlocale6.pc.in
@@ -0,0 +1,10 @@
+prefix="@CMAKE_INSTALL_PREFIX@"
+exec_prefix="${prefix}"
+libdir="${prefix}/lib"
+includedir="${prefix}/include/mlocale6"
+
+Name: @PROJECT_NAME@
+Description: @CMAKE_PROJECT_DESCRIPTION@
+Version: @PROJECT_VERSION@
+Cflags: -I${includedir}
+Libs: -L${libdir} -l@target@
