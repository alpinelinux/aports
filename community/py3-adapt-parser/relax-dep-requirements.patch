Upstream: no

We can't ship specifically pyee 7.0.1, we're not a stable release distribution, so relax the requirements

diff --git a/setup.py b/setup.py
index 71296ba..a3e0313 100644
--- a/setup.py
+++ b/setup.py
@@ -29,7 +29,7 @@ setup(
     packages = ["adapt", "adapt.tools", "adapt.tools.text"],
 
     install_requires = [
-        "pyee==7.0.1",
+        "pyee>=7.0.1",
         "six>=1.10.0"
     ]
 )
