# NLS detection is broken for musl, so force it on
diff --git a/configure b/configure
index c1380c3..9b8c9c8 100755
--- a/configure
+++ b/configure
@@ -6169,6 +6169,8 @@ $as_echo "#define ENABLE_NLS 1" >>confdefs.h
       USE_NLS=no
     fi
   fi
+# Force NLS support for Alpine/musl
+USE_NLS=yes
 
   { $as_echo "$as_me:${as_lineno-$LINENO}: checking whether to use NLS" >&5
 $as_echo_n "checking whether to use NLS... " >&6; }
