# Contributor: Will Sinatra <wpsinatra@gmail.com>
# Maintainer: Will Sinatra <wpsinatra@gmail.com>
pkgname=ruby-simplecov
_gemname=${pkgname#ruby-}
pkgver=1.3.0
pkgrel=0
pkgdesc="Code coverage for Ruby"
url="https://rubygems.org/gems/simplecov"
arch="noarch"
license="MIT"
depends="ruby"
checkdepends="ruby-rake ruby-bundler ruby-rspec"
depends="ruby-docile ruby-simplecov-html ruby-simplecov_json_formatter"
source="$pkgname-$pkgver.tar.gz::https://github.com/simplecov-ruby/simplecov/archive/refs/tags/v$pkgver.tar.gz"
builddir="$srcdir/$_gemname-$pkgver"
subpackages="$pkgname-doc"
options="!check" #depends on rubocop

prepare() {
	default_prepare

	sed -i '/spec.signing_key/d' $_gemname.gemspec
}

build() {
	gem build $_gemname.gemspec
}

check() {
	rake test
}

package() {
	local gemdir="$pkgdir$(ruby -e 'puts Gem.default_dir')"

	gem install --local \
		--install-dir "$gemdir" \
		--ignore-dependencies \
		--no-document \
		--verbose \
		$_gemname

	mkdir -p "$pkgdir"/usr/share/doc/"$pkgname"/
	install -Dm644 "$builddir"/docs/*.md "$pkgdir"/usr/share/doc/"$pkgname"/

	rm -r "$gemdir"/cache \
		"$gemdir"/build_info

	cd "$gemdir/gems/$_gemname-$pkgver"
	rm -r README.md

	install -Dm644 "$builddir"/LICENSE "$pkgdir"/usr/share/licenses/"$pkgname"/LICENSE
}

sha512sums="
7d34bd7450e4970eec5e2f01f1ff4995e299cff433bf85662465d670d92867e9e8841bdadb8304048d577a13d353058e769cf29467b085fe5c13fb3483fa1d5a  ruby-simplecov-1.3.0.tar.gz
"
