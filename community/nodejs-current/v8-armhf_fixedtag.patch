From: Johannes Müller <alpinelinux@jogemu.at>
Date: Wed May 6 19:56:53 2025 +0000
Subject: Allow less highway lanes to fix armhf build

--- a/deps/v8/src/strings/string-hasher.cc
+++ b/deps/v8/src/strings/string-hasher.cc
@@ -77,7 +77,7 @@
 
 bool IsOnly8BitSIMD(const uint16_t* chars, unsigned len) {
   namespace hw = hwy::HWY_NAMESPACE;
-  hw::FixedTag<uint16_t, 8> tag;
+  hw::CappedTag<uint16_t, 8> tag;
   const size_t stride = hw::Lanes(tag);
   const auto high_byte_mask = hw::Set(tag, static_cast<uint16_t>(0xFF00));
   const auto zero = hw::Zero(tag);
--- a/deps/v8/src/json/json-stringifier.cc
+++ b/deps/v8/src/json/json-stringifier.cc
@@ -3398,7 +3398,7 @@
   size_t uncopied_src_index = 0;  // Index of first char not copied yet.
   const SrcChar* block = chars;
   const SrcChar* end = chars + length;
-  hw::FixedTag<SrcChar, 16> tag;
+  hw::CappedTag<SrcChar, 16> tag;
   static const size_t stride = hw::Lanes(tag);
 
   const auto mask_0x20 = hw::Set(tag, 0x20);
