#!/bin/sh

cat 1>&2 <<EOF

NOTE: This package is the old GNOME Boxes implementation using GTK 3.
It is in low-maintenance mode and no longer receives active feature updates.
Active development has moved to a new Flatpak-based code base: <https://gitlab.gnome.org/felipeborges/boxes>

EOF
