# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-launcher
pkgver=1.0.11
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Application launcher for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-launcher"
# riscv64: too slow to build
arch="all !riscv64"
license="GPL-3.0-or-later"
depends="
	pop-launcher
"
makedepends="
	appstream-glib
	cargo-auditable
	desktop-file-utils
	intltool
	just
	libxkbcommon-dev
	wayland-dev
	wayland-protocols
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-launcher/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CHOST" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
1ee0a7bc84f8bcbbef82a4b761dc9ab045ad79b54c1a04b6d9cf5eeacf1314157aa880dfe574c358e2c1877d35ba669e6d269e081068cf8bdd8b470ceaa60949  cosmic-launcher-1.0.11.tar.gz
"
