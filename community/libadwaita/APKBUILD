# Contributor: Adam Plumb <adamplumb@gmail.com>
# Maintainer: team/gnome <achill@achill.org>
pkgname=libadwaita
pkgver=1.8.1
pkgrel=0
pkgdesc="Building blocks for modern GNOME applications"
url="https://gitlab.gnome.org/GNOME/libadwaita"
arch="all"
license="LGPL-2.1-or-later"
depends="adwaita-fonts"
makedepends="
	appstream-dev
	fribidi-dev
	glib-dev
	gobject-introspection-dev
	gtk4.0-dev
	meson
	sassc
	vala
	"
# Tests need any font
checkdepends="
	dbus
	font-dejavu
	mesa-dri-gallium
	xvfb-run
	"
subpackages="$pkgname-dbg $pkgname-dev $pkgname-lang $pkgname-demo"
source="https://download.gnome.org/sources/libadwaita/${pkgver%.*}/libadwaita-$pkgver.tar.xz"

case "$CARCH" in
arm*) options="!check" ;; # race condition
esac

build() {
	CFLAGS="$CFLAGS -g1" \
	CPPFLAGS="$CPPFLAGS -g1" \
	abuild-meson \
		-Db_lto=true \
		-Dexamples=true \
		-Ddocumentation=false \
		. output
	meson compile -C output
}

check() {
	GTK_THEME=Adwaita \
	XDG_RUNTIME_DIR="$(mktemp -d -p "$builddir")" \
	dbus-run-session -- \
	xvfb-run -a \
	meson test --print-errorlogs -C output
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

demo() {
	pkgdesc="$pkgdesc (libadwaita demo)"
	amove usr/share usr/bin
}

sha512sums="
7c47a1ced50a24245769d1e87797aa8f5da881ebab97fecccc5d5fb1d8b0ec1cd388cc0610f971d326065a9967250690fcd611d7d3602e300d5e9c1a206e120d  libadwaita-1.8.1.tar.xz
"
