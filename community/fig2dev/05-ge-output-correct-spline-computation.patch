Patch-Source: https://sourceforge.net/p/mcj/fig2dev/ci/2bd6c0b210916d0d3ca81f304535b5af0849aa93/
Upstream-Issue: https://sourceforge.net/p/mcj/tickets/190/
CVE-ID: CVE-2025-46399
---
--- a/fig2dev/dev/genge.c
+++ b/fig2dev/dev/genge.c
@@ -229,8 +229,6 @@ genge_itp_spline(F_spline *s)
 	int		 xmin, ymin;
 
 	a = s->controls;
-
-	a = s->controls;
 	p = s->points;
 	/* go through the points to find the last two */
 	for (q = p->next; q != NULL; p = q, q = q->next) {
@@ -238,6 +236,7 @@ genge_itp_spline(F_spline *s)
 	    a = b;
 	}
 
+	a = s->controls;
 	p = s->points;
 	fprintf(tfp, "n %d %d m\n", p->x, p->y);
 	xmin = 999999;
