# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=pop-launcher
pkgver=1.8.0
pkgrel=0
pkgdesc="Modular IPC-based desktop launcher service"
url="https://github.com/pop-os/launcher"
# riscv64: too slow to build
arch="all !riscv64"
license="MPL-2.0 AND GPL-3.0-only"
depends="
	fd
	xdg-utils
"
makedepends="
	cargo
	coreutils
	clang-libclang
	dbus-dev
	just
	libxkbcommon-dev
	mesa-dev
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/launcher/archive/refs/tags/epoch-$pkgver.tar.gz"
builddir="$srcdir/launcher-epoch-$pkgver"

prepare() {
	default_prepare

	cargo fetch --target="$CTARGET" --locked
}

build() {
	just build-release --frozen
}

check() {
	cargo test --release --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
f8a78b56404ffe3810c0fe45476acfbdffff236dd2842e2ce27fd0fde2b7f2dc083ad5d3ffe7f35b25ad2ee4c1b4b7733ff442803896958e39d486b1e6bcf0b9  pop-launcher-1.8.0.tar.gz
"
