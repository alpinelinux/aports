--- a/testutil/socket.go	2025-12-08 18:54:34.000000000 +0000
+++ b/testutil/socket.go	2026-03-08 05:53:40.840000000 +0000
@@ -27,6 +27,12 @@ func TempSocket(tb testing.TB) string {
 
 		return sock
 	}
+	filename := "TempSocket"
+	sock := filepath.Join("/tmp", filename)
 
-	return filepath.Join(tb.TempDir(), "sock")
+	tb.Cleanup(func() {
+		require.NoError(tb, os.RemoveAll(sock))
+	})
+
+	return sock
 }
