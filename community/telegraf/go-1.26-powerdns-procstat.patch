--- a/plugins/inputs/powerdns/powerdns_linux_test.go	2025-12-08 18:54:34.000000000 +0000
+++ b/plugins/inputs/powerdns/powerdns_linux_test.go	2026-03-08 03:28:29.590000000 +0000
@@ -36,7 +36,7 @@ func serverSocket(l net.Listener) {
 func TestPowerdnsGeneratesMetrics(t *testing.T) {
 	// We create a fake server to return test data
 	randomNumber := int64(5239846799706671610)
-	sockname := filepath.Join(t.TempDir(), fmt.Sprintf("pdns%d.controlsocket", randomNumber))
+	sockname := filepath.Join("/tmp", fmt.Sprintf("pdns%d.controlsocket", randomNumber))
 	socket, err := net.Listen("unix", sockname)
 	if err != nil {
 		t.Fatal("Cannot initialize server on port ")
--- a/plugins/inputs/procstat/os_linux.go	2025-12-08 18:54:34.000000000 +0000
+++ b/plugins/inputs/procstat/os_linux.go	2026-03-08 03:17:42.760000000 +0000
@@ -96,11 +96,11 @@ func findBySystemdUnits(units []string)
 		raw := prop.Value.Value()
 		pid, ok := raw.(uint32)
 		if !ok {
-			return nil, fmt.Errorf("failed to parse PID %v of unit %q: invalid type %T", raw, u, raw)
+			return nil, fmt.Errorf("failed to parse PID %v of unit %v: invalid type %T", raw, u, raw)
 		}
 		p, err := gopsprocess.NewProcess(int32(pid))
 		if err != nil {
-			return nil, fmt.Errorf("failed to find process for PID %d of unit %q: %w", pid, u, err)
+			return nil, fmt.Errorf("failed to find process for PID %d of unit %v: %w", pid, u, err)
 		}
 		groups = append(groups, processGroup{
 			processes: []*gopsprocess.Process{p},
