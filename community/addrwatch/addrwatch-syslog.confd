# Configuration for /etc/init.d/addrwatch-syslog

# User (and group) to run addrwatch_syslog as.
#command_user="addrwatch"

# Additional arguments for addrwatch_syslog daemon.
#command_args=

# Log messages are redirected to syslog. Set to empty string to disable.
#error_logger="logger -t addrwatch-syslog -p daemon.err >/dev/null 2>&1"

# Comment out to disable process supervisor.
supervisor=supervise-daemon
