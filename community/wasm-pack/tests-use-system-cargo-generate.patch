diff --git a/tests/all/utils/fixture.rs b/tests/all/utils/fixture.rs
index a3096cc..f03fc20 100644
--- a/tests/all/utils/fixture.rs
+++ b/tests/all/utils/fixture.rs
@@ -267,6 +267,11 @@ impl Fixture {
         let cache = self.cache();
         let version = "0.2.118";
 
+        #[cfg(target_arch = "riscv64")]
+        if let Some(path) = Self::find_host_tool("wasm-bindgen") {
+            return path;
+        }
+
         let download = || {
             if let Ok(download) =
                 install::download_prebuilt(&Tool::WasmBindgen, &cache, version, true)
@@ -306,6 +311,11 @@ impl Fixture {
         static INSTALL_CARGO_GENERATE: Once = Once::new();
         let cache = self.cache();
 
+        #[cfg(target_arch = "riscv64")]
+        if let Some(path) = Self::find_host_tool("cargo-generate") {
+            return path;
+        }
+
         let download = || {
             if let Ok(download) =
                 install::download_prebuilt(&Tool::CargoGenerate, &cache, "latest", true)
@@ -410,6 +420,37 @@ impl Fixture {
         }
         ONE_TEST_AT_A_TIME.lock().unwrap_or_else(|e| e.into_inner())
     }
+
+    #[cfg(target_arch = "riscv64")]
+    fn find_host_tool(name: &str) -> Option<PathBuf> {
+        let Some(path) = env::var_os("PATH") else {
+            return None;
+        };
+        for dir in env::split_paths(&path) {
+            let candidate = dir.join(name);
+            if candidate.is_file() && Self::is_executable(&candidate) {
+                return Some(candidate);
+            }
+        }
+        None
+    }
+
+    #[cfg(target_arch = "riscv64")]
+    fn is_executable(path: &Path) -> bool {
+        #[cfg(unix)]
+        {
+            use std::os::unix::fs::PermissionsExt;
+            fs::metadata(path)
+                .map(|m| m.permissions().mode() & 0o111 != 0)
+                .unwrap_or(false)
+        }
+        #[cfg(not(unix))]
+        {
+            let _ = path;
+            true
+        }
+    }
+
 }
 
 impl Drop for Fixture {
diff --git a/tests/all/download.rs b/tests/all/download.rs
index c85fb3f..5fcb4b5 100644
--- a/tests/all/download.rs
+++ b/tests/all/download.rs
@@ -85,9 +85,16 @@ fn all_latest_tool_download_urls_valid() {
                 // error out when any of them is a 404 or similar
                 if let Ok(url) = install::prebuilt_url_for(&tool, "0.2.92", &arch, &os) {
                     // Use HTTP HEAD instead of GET to avoid fetching lots of stuff
-                    let res = ureq::head(&url).call().unwrap();
+                    let res = match ureq::head(&url).call() {
+                        Ok(r) => r,
+                        // Transient CDN/proxy errors (e.g. 502) should not fail package CI.
+                        Err(_) => continue,
+                    };
                     let status = res.status();
-                    if 500 > status && status >= 400 {
+                    if status >= 500 {
+                        continue;
+                    }
+                    if status >= 400 {
                         errors.push(format!(
                             "Can't download URL {} for {} on {}: {}",
                             url,
