Not using glibc binaries

--- a/tests/all/test.rs
+++ b/tests/all/test.rs
@@ -42,8 +42,8 @@ fn it_can_run_browser_tests() {
     fixture.install_local_wasm_bindgen();
 
     let firefox = cfg!(any(
-        all(target_os = "linux", target_arch = "x86"),
-        all(target_os = "linux", target_arch = "x86_64"),
+        all(target_os = "linux", target_arch = "x86", not(target_env = "musl")),
+        all(target_os = "linux", target_arch = "x86_64", not(target_env = "musl")),
         all(target_os = "macos", target_arch = "x86_64"),
         all(target_os = "macos", target_arch = "aarch64"),
         all(target_os = "windows", target_arch = "x86"),
@@ -54,7 +54,7 @@ fn it_can_run_browser_tests() {
     }
 
     let chrome = cfg!(any(
-        all(target_os = "linux", target_arch = "x86_64"),
+        all(target_os = "linux", target_arch = "x86_64", not(target_env = "musl")),
         all(target_os = "macos", target_arch = "x86_64"),
         all(target_os = "windows", target_arch = "x86")
     ));
@@ -103,8 +103,8 @@ fn it_can_run_failing_tests() {
 
 #[test]
 #[cfg(any(
-    all(target_os = "linux", target_arch = "x86"),
-    all(target_os = "linux", target_arch = "x86_64"),
+    all(target_os = "linux", target_arch = "x86", not(target_env = "musl")),
+    all(target_os = "linux", target_arch = "x86_64", not(target_env = "musl")),
     all(target_os = "macos", target_arch = "x86_64"),
     all(target_os = "macos", target_arch = "aarch64"),
     all(target_os = "windows", target_arch = "x86"),
