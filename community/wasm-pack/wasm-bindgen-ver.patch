diff --git a/tests/all/lockfile.rs b/tests/all/lockfile.rs
index db29227..0a0a6e0 100644
--- a/tests/all/lockfile.rs
+++ b/tests/all/lockfile.rs
@@ -9,7 +9,7 @@ fn it_gets_wasm_bindgen_version() {
     fixture.cargo_check();
     let data = CrateData::new(&fixture.path, None).unwrap();
     let lock = Lockfile::new(&data).unwrap();
-    assert_eq!(lock.wasm_bindgen_version(), Some("0.2.100"),);
+    assert_eq!(lock.wasm_bindgen_version(), Some("0.2.118"),);
 }
 
 #[test]
@@ -48,7 +48,7 @@ fn it_gets_wasm_bindgen_version_in_crate_inside_workspace() {
                 crate-type = ["cdylib"]
 
                 [dependencies]
-                wasm-bindgen = "=0.2.100"
+                wasm-bindgen = "=0.2.118"
             "#,
         )
         .file(
@@ -64,7 +64,7 @@ fn it_gets_wasm_bindgen_version_in_crate_inside_workspace() {
     fixture.cargo_check();
     let data = CrateData::new(&fixture.path.join("blah"), None).unwrap();
     let lock = Lockfile::new(&data).unwrap();
-    assert_eq!(lock.wasm_bindgen_version(), Some("0.2.100"),);
+    assert_eq!(lock.wasm_bindgen_version(), Some("0.2.118"),);
 }
 
 #[test]
@@ -93,7 +93,7 @@ fn it_gets_wasm_bindgen_version_from_dependencies() {
                 crate-type = ["cdylib"]
 
                 [dependencies]
-                wasm-bindgen = "=0.2.100"
+                wasm-bindgen = "=0.2.118"
             "#,
         )
         .file(
@@ -132,5 +132,5 @@ fn it_gets_wasm_bindgen_version_from_dependencies() {
     fixture.cargo_check();
     let data = CrateData::new(&fixture.path.join("parent"), None).unwrap();
     let lock = Lockfile::new(&data).unwrap();
-    assert_eq!(lock.wasm_bindgen_version(), Some("0.2.100"),);
+    assert_eq!(lock.wasm_bindgen_version(), Some("0.2.118"),);
 }
diff --git a/tests/all/readme.rs b/tests/all/readme.rs
index 61f6307..7e26a6f 100644
--- a/tests/all/readme.rs
+++ b/tests/all/readme.rs
@@ -60,7 +60,7 @@ fn it_copies_a_readme_provided_path() {
             # bindgen downloaded is what we expect, and if `=` is
             # removed then it will download whatever the newest version
             # of wasm-bindgen is which may not be what's listed here.
-            wasm-bindgen = "=0.2.100"
+            wasm-bindgen = "=0.2.118"
 
             [dev-dependencies]
             wasm-bindgen-test = "0.3"
@@ -120,7 +120,7 @@ fn it_ignores_a_disabled_readme() {
             # bindgen downloaded is what we expect, and if `=` is
             # removed then it will download whatever the newest version
             # of wasm-bindgen is which may not be what's listed here.
-            wasm-bindgen = "=0.2.100"
+            wasm-bindgen = "=0.2.118"
 
             [dev-dependencies]
             wasm-bindgen-test = "0.3"
diff --git a/tests/all/utils/fixture.rs b/tests/all/utils/fixture.rs
index 61eb7ed..a3096cc 100644
--- a/tests/all/utils/fixture.rs
+++ b/tests/all/utils/fixture.rs
@@ -144,7 +144,7 @@ impl Fixture {
                     # bindgen downloaded is what we expect, and if `=` is
                     # removed then it will download whatever the newest version
                     # of wasm-bindgen is which may not be what's listed here.
-                    wasm-bindgen = "=0.2.100"
+                    wasm-bindgen = "=0.2.118"
 
                     [dev-dependencies]
                     wasm-bindgen-test = "0.3"
@@ -182,7 +182,7 @@ impl Fixture {
                     # bindgen downloaded is what we expect, and if `=` is
                     # removed then it will download whatever the newest version
                     # of wasm-bindgen is which may not be what's listed here.
-                    wasm-bindgen = "=0.2.100"
+                    wasm-bindgen = "=0.2.118"
 
                     [dev-dependencies]
                     wasm-bindgen-test = "0.3"
@@ -220,7 +220,7 @@ impl Fixture {
                     crate-type = ["cdylib"]
 
                     [dependencies]
-                    wasm-bindgen = "=0.2.100"
+                    wasm-bindgen = "=0.2.118"
 
                     [dev-dependencies]
                     wasm-bindgen-test = "=0.2.21"
@@ -265,7 +265,7 @@ impl Fixture {
 
         static INSTALL_WASM_BINDGEN: Once = Once::new();
         let cache = self.cache();
-        let version = "0.2.100";
+        let version = "0.2.118";
 
         let download = || {
             if let Ok(download) =
@@ -475,7 +475,7 @@ pub fn no_cdylib() -> Fixture {
             # crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
 
             [dev-dependencies]
             wasm-bindgen-test = "0.3"
@@ -528,7 +528,7 @@ pub fn wbg_test_diff_versions() -> Fixture {
 
                 [dependencies]
                 # We depend on the latest wasm-bindgen 0.2
-                wasm-bindgen = "0.2"
+                wasm-bindgen = "=0.2.118"
 
                 [dev-dependencies]
                 # And we depend on wasm-bindgen-test 0.2.29. This should still
@@ -646,7 +646,7 @@ pub fn transitive_dependencies() -> Fixture {
             crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
             project_a = { path = "../project_a" }
             project_b = { path = "../project_b" }
 
@@ -696,7 +696,7 @@ pub fn transitive_dependencies() -> Fixture {
             crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
             project_b = { path = "../project_b" }
 
             [dev-dependencies]
@@ -746,7 +746,7 @@ pub fn transitive_dependencies() -> Fixture {
             crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
 
             [dev-dependencies]
             wasm-bindgen-test = "0.3"
diff --git a/tests/all/download.rs b/tests/all/download.rs
index 7042daa..c85fb3f 100644
--- a/tests/all/download.rs
+++ b/tests/all/download.rs
@@ -10,7 +10,7 @@ fn can_download_prebuilt_wasm_bindgen() {
     let dir = tempfile::TempDir::new().unwrap();
     let cache = binary_install::Cache::at(dir.path());
     if let install::Status::Found(dl) =
-        install::download_prebuilt(&Tool::WasmBindgen, &cache, "0.2.100", true).unwrap()
+        install::download_prebuilt(&Tool::WasmBindgen, &cache, "0.2.118", true).unwrap()
     {
         assert!(dl.binary("wasm-bindgen").unwrap().is_file());
         assert!(dl.binary("wasm-bindgen-test-runner").unwrap().is_file())
diff --git a/tests/all/build.rs b/tests/all/build.rs
index c588d6b..27b8c1b 100644
--- a/tests/all/build.rs
+++ b/tests/all/build.rs
@@ -2,6 +2,7 @@ use crate::utils;
 use assert_cmd::prelude::*;
 use std::fs;
 use std::path::Path;
+use std::process::Command;
 
 #[test]
 fn build_in_non_crate_directory_doesnt_panic() {
@@ -77,7 +78,7 @@ fn it_should_build_crates_in_a_workspace() {
                 crate-type = ["cdylib"]
 
                 [dependencies]
-                wasm-bindgen = "0.2"
+                wasm-bindgen = "=0.2.118"
             "#,
         )
         .file(
@@ -117,7 +118,7 @@ fn renamed_crate_name_works() {
                 name = 'bar'
 
                 [dependencies]
-                wasm-bindgen = "0.2"
+                wasm-bindgen = "=0.2.118"
             "#,
         )
         .file(
@@ -250,7 +251,7 @@ fn build_with_and_without_wasm_bindgen_debug() {
                     crate-type = ["cdylib"]
 
                     [dependencies]
-                    wasm-bindgen = "0.2"
+                    wasm-bindgen = "=0.2.118"
 
                     [package.metadata.wasm-pack.profile.dev.wasm-bindgen]
                     debug-js-glue = {}
@@ -347,6 +348,32 @@ fn build_from_new() {
     let name = "generated-project";
     fixture.wasm_pack().arg("new").arg(name).assert().success();
     let project_location = fixture.path.join(&name);
+    let status = Command::new("cargo")
+        .current_dir(&project_location)
+        .args([
+            "update",
+            "-p",
+            "wasm-bindgen-test",
+            "--precise",
+            "0.3.68",
+        ])
+        .status()
+        .expect("failed to run cargo update for wasm-bindgen-test");
+    assert!(status.success(), "cargo update failed: {status:?}");
+
+    let status = Command::new("cargo")
+        .current_dir(&project_location)
+        .args([
+            "update",
+            "-p",
+            "wasm-bindgen",
+            "--precise",
+            "0.2.118",
+        ])
+        .status()
+        .expect("failed to run cargo update for wasm-bindgen");
+    assert!(status.success(), "cargo update failed: {status:?}");
+
     fixture
         .wasm_pack()
         .arg("build")
@@ -375,7 +402,7 @@ fn build_crates_with_same_names() {
             crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
             somenameother = { path = "../somename2", package = "somename" }
             "#,
         )
diff --git a/tests/all/manifest.rs b/tests/all/manifest.rs
index f1771dd..0e586c8 100644
--- a/tests/all/manifest.rs
+++ b/tests/all/manifest.rs
@@ -530,7 +530,7 @@ fn configure_wasm_bindgen_debug_incorrectly_is_error() {
             crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
 
             [package.metadata.wasm-pack.profile.dev.wasm-bindgen]
             debug-js-glue = "not a boolean"
@@ -564,7 +564,7 @@ fn parse_crate_data_returns_unused_keys_in_cargo_toml() {
             crate-type = ["cdylib"]
 
             [dependencies]
-            wasm-bindgen = "0.2"
+            wasm-bindgen = "=0.2.118"
 
             # Note: production is not valid.
             [package.metadata.wasm-pack.profile.production.wasm-bindgen]
