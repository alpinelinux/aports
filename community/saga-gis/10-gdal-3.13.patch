Author: Holger Jaekel <holger.jaekel@gmx.de>
Summary: GDALGetMetadata() now returns a CSLConstList (const char * const *) instead of char **
----

--- a/src/tools/io/io_gdal/ogr_driver.cpp
+++ b/src/tools/io/io_gdal/ogr_driver.cpp
@@ -572,7 +572,7 @@
 
 	if( pLayer != NULL )
 	{
-		char **pMetaData = GDALGetMetadata(pLayer, 0);
+		const char* const* pMetaData = GDALGetMetadata(pLayer, 0);
 
 		if( pMetaData )
 		{




--- a/src/tools/io/io_gdal/gdal_driver.cpp
+++ b/src/tools/io/io_gdal/gdal_driver.cpp
@@ -746,7 +746,7 @@
 {
 	if( m_pDataSet && is_Reading() )
 	{
-		char	**pMetaData	= GDALGetMetadata(m_pDataSet, 0);
+		const char* const* pMetaData	= GDALGetMetadata(m_pDataSet, 0);
 
 		if( pMetaData )
 		{
@@ -774,7 +774,7 @@
 {
 	if( m_pDataSet && is_Reading() )
 	{
-		char	**pMetaData	= GDALGetMetadata(m_pDataSet, pszDomain);
+		const char* const* pMetaData	= GDALGetMetadata(m_pDataSet, pszDomain);
 
 		if( pMetaData )
 		{
@@ -892,7 +892,7 @@
 
 	GDALRasterBandH	pBand = is_Reading() ? GDALGetRasterBand(m_pDataSet, i + 1) : NULL;
 
-	char **pMetaData = pBand ? GDALGetMetadata(pBand, NULL) : NULL;
+	const char* const* pMetaData = pBand ? GDALGetMetadata(pBand, NULL) : NULL;
 
 	if( pMetaData )
 	{
@@ -917,7 +917,7 @@
 {
 	GDALRasterBandH	pBand = is_Reading() ? GDALGetRasterBand(m_pDataSet, i + 1) : NULL;
 
-	char **pMetaData = pBand ? GDALGetMetadata(pBand, NULL) : NULL;
+	const char* const* pMetaData = pBand ? GDALGetMetadata(pBand, NULL) : NULL;
 
 	if( pMetaData )
 	{
