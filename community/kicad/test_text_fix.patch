diff --git a/qa/tests/common/text_eval/test_text_eval_parser_integration.cpp b/qa/tests/common/text_eval/test_text_eval_parser_integration.cpp
index 6d17229047..89cb66dc1d 100644
--- a/qa/tests/common/text_eval/test_text_eval_parser_integration.cpp
+++ b/qa/tests/common/text_eval/test_text_eval_parser_integration.cpp
@@ -427,7 +427,7 @@ BOOST_AUTO_TEST_CASE( RealWorldPerformance )
     auto duration = std::chrono::duration_cast<std::chrono::milliseconds>( end - start );
 
-    // Should process 1000 expressions in reasonable time (less than 100 ms)
+    // Should process 1000 expressions in reasonable time (less than 300 ms)
-    BOOST_CHECK_LT( duration.count(), 100 );
+    BOOST_CHECK_LT( duration.count(), 300 );
 
     // Test that results are consistent
     for( auto& expr : expressions )
--- a/qa/tests/eeschema/test_annotation_units_integration.cpp	2026-07-21 22:13:55.000000000 +0300
+++ b/qa/tests/eeschema/test_annotation_units_integration.cpp	2026-07-30 18:47:56.037030155 +0300
@@ -354,14 +354,14 @@
         int result1 = tracker->GetNextRefDesForUnits( rRef, emptyMap, emptyUnits, 1 );
         int result50 = tracker->GetNextRefDesForUnits( rRef, emptyMap, emptyUnits, 50 );
     }
 
     auto end = std::chrono::high_resolution_clock::now();
     auto duration = std::chrono::duration_cast<std::chrono::microseconds>( end - start );
 
     BOOST_TEST_MESSAGE( "200 GetNextRefDesForUnits calls took: " + std::to_string( duration.count() ) + " microseconds" );
 
     // The actual time will vary, but the calls should complete in reasonable time
-    BOOST_CHECK_LT( duration.count(), 50000 ); // Should be under 50ms for 200 calls
+    BOOST_CHECK_LT( duration.count(), 150000 ); // Should be under 150ms for 200 calls
 }
 
-BOOST_AUTO_TEST_SUITE_END()
\ No newline at end of file
+BOOST_AUTO_TEST_SUITE_END()
