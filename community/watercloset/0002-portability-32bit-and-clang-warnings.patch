--- a/src/mapEditor.c
+++ b/src/mapEditor.c
@@ -34,7 +34,7 @@
 static void saveMap(cJSON *root)
 {
 	int x, y;
-	unsigned long l;
+	size_t l;
 	FILE *fp;
 	char *buff;
 
--- a/src/world/quadtree.c
+++ b/src/world/quadtree.c
@@ -109,7 +109,7 @@
 
 	if (root->depth == 0)
 	{
-		SDL_LogMessage(SDL_LOG_CATEGORY_APPLICATION, SDL_LOG_PRIORITY_DEBUG, "Quadtree: [totalDepth = %d, numCells = %d, memory = %ldkb]\n", totalDepth, numCells, (sizeof(Quadtree) * numCells) / 1024);
+		SDL_LogMessage(SDL_LOG_CATEGORY_APPLICATION, SDL_LOG_PRIORITY_DEBUG, "Quadtree: [totalDepth = %d, numCells = %d, memory = %zukb]\n", totalDepth, numCells, (sizeof(Quadtree) * numCells) / 1024);
 	}
 }
 
--- a/src/world/stage.c
+++ b/src/world/stage.c
@@ -399,7 +399,7 @@
 	drawWipe();
 }
 
-static void drawGame()
+static void drawGame(void)
 {
 	app.dev.drawing = 0;
 
@@ -423,7 +423,7 @@
 	}
 }
 
-static void drawMenu()
+static void drawMenu(void)
 {
 	drawGame();
 
