diff --git a/version/build.rs b/version/build.rs
index 3af50bf..eb1d4c6 100644
--- a/version/build.rs
+++ b/version/build.rs
@@ -10,17 +10,7 @@ const CHANGES_INDEX_URL: &str = "https://faircamp.org/changes/";
 /// If git is not available, we fall back to "unknown revision".
 /// The output is stored in FAIRCAMP_REVISION.
 fn compute_revision() {
-    let mut git = Command::new("git");
-
-    git.args(["rev-parse", "--short", "HEAD"]);
-
-    let revision = if let Ok(output) = git.output() && output.status.success() {
-        // We trim this because the output includes a trailing newline
-        String::from_utf8(output.stdout).unwrap().trim().into()
-    } else {
-        String::from("unknown revision")
-    };
-
+    let revision = std::env::var("FAIRCAMP_REVISION").unwrap_or("unknown revision".into());
     println!("cargo:rustc-env=FAIRCAMP_REVISION={revision}");
 }
 
