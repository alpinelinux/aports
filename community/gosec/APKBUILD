# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=gosec
pkgver=2.23.0
pkgrel=0
pkgdesc="Go source code static analyzer, focusing on security"
url="https://github.com/securego/gosec"
arch="all"
license="Apache-2.0"
makedepends="go"
source="$pkgname-$pkgver.tar.gz::https://github.com/securego/gosec/archive/refs/tags/v$pkgver.tar.gz"
# downloads go deps
options="net"

export GOCACHE="${GOCACHE:-"$srcdir/go-cache"}"
export GOTMPDIR="${GOTMPDIR:-"$srcdir"}"
export GOMODCACHE="${GOMODCACHE:-"$srcdir/go"}"

build() {
	make PREFIX="/usr" VERSION="$pkgver"
}

check() {
	go run github.com/onsi/ginkgo/v2/ginkgo -- --ginkgo.v --ginkgo.fail-fast
}

package() {
	install -Dm755 gosec -t "$pkgdir"/usr/bin
}

sha512sums="
e38d937020a2158e022a49ab32b3db5785a8ce5da5085c1ced0c8ea9ff478fb532b9431e8eaa6795c62fb0909923c7f7a8255f30f34c3d129a8715575f9debd0  gosec-2.23.0.tar.gz
"
