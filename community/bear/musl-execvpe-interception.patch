--- a/intercept-preload/build.rs
+++ b/intercept-preload/build.rs
@@ -34,11 +34,17 @@
     if cfg!(target_family = "unix") {
         let out_dir = std::env::var("OUT_DIR").unwrap();
 
-        let intercept_symbols: Vec<&str> = platform_checks::DETECTED_SYMBOLS
+        let mut intercept_symbols: Vec<&str> = platform_checks::DETECTED_SYMBOLS
             .iter()
             .copied()
             .filter(|s| INTERCEPT_FAMILY.contains(s))
             .collect();
+        #[cfg(target_env = "musl")]
+        {
+            // musl defines execvpe as weak_alias(__execvpe, execvpe). Some
+            // linkers bind callers to __execvpe directly; export it for LD_PRELOAD.
+            intercept_symbols.push("__execvpe");
+        }
 
         // Compile the C shim for all intercepted functions
         // This handles variadic arguments properly (execl family) and provides

--- a/intercept-preload/src/c/shim.c
+++ b/intercept-preload/src/c/shim.c
@@ -264,6 +264,15 @@
 }
 #endif
 
+// musl implements execvpe as a weak alias of __execvpe. Export the real
+// symbol so LD_PRELOAD interception works when the linker binds to __execvpe.
+#if defined(has_symbol_execvpe)
+EXPORT int __execvpe(const char *file, char *const argv[], char *const envp[])
+{
+    return rust_execvpe(file, argv, envp);
+}
+#endif
+
 //
 // execvP - execute a file with custom search path (BSD extension)
 //

--- a/intercept-preload/src/implementation.rs
+++ b/intercept-preload/src/implementation.rs
@@ -45,7 +45,21 @@
 
 #[ctor]
 static REAL_EXECVPE: AtomicPtr<libc::c_void> = {
-    let ptr = unsafe { libc::dlsym(RTLD_NEXT, c"execvpe".as_ptr() as *const _) };
+    let ptr = unsafe {
+        #[cfg(target_env = "musl")]
+        {
+            // Resolve the real musl implementation: try execvpe first, then
+            // __execvpe (s390x RTLD_NEXT often only exposes execvpe).
+            let ptr = libc::dlsym(RTLD_NEXT, c"execvpe".as_ptr() as *const _);
+            if ptr.is_null() {
+                libc::dlsym(RTLD_NEXT, c"__execvpe".as_ptr() as *const _)
+            } else {
+                ptr
+            }
+        }
+        #[cfg(not(target_env = "musl"))]
+        { libc::dlsym(RTLD_NEXT, c"execvpe".as_ptr() as *const _) }
+    };
     AtomicPtr::new(ptr)
 };
 

