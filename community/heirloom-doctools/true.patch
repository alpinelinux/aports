diff --git a/troff/n5.c b/troff/n5.c
index df7cf01..e592e0b 100644
--- a/troff/n5.c
+++ b/troff/n5.c
@@ -1594,7 +1594,7 @@ void
 caseif(int x)
 {
 	extern int falsef;
-	register int notflag, true;
+	register int notflag, trueflag;
 	tchar i, j;
 	enum warn w = warn;
 	int	flt = 0;
@@ -1604,11 +1604,11 @@ caseif(int x)
 		goto i2;
 	if (x == 2) {
 		notflag = 0;
-		true = el;
+		trueflag = el;
 		el = 0;
 		goto i1;
 	}
-	true = 0;
+	trueflag = 0;
 	skip(1);
 	if ((cbits(i = getch())) == '!') {
 		notflag = 1;
@@ -1631,27 +1631,27 @@ caseif(int x)
 		i = (int)atoi0();
 	if (!nonumb) {
 		if (i > 0)
-			true++;
+			trueflag++;
 		goto i1;
 	}
 	i = getch();
 	switch (cbits(i)) {
 	case 'e':
 		if (!(numtab[PN].val & 01))
-			true++;
+			trueflag++;
 		break;
 	case 'o':
 		if (numtab[PN].val & 01)
-			true++;
+			trueflag++;
 		break;
 #ifdef NROFF
 	case 'n':
-		true++;
+		trueflag++;
 	case 't':
 #endif
 #ifndef NROFF
 	case 't':
-		true++;
+		trueflag++;
 	case 'n':
 #endif
 		break;
@@ -1662,7 +1662,7 @@ caseif(int x)
 		tryglf++;
 		if (!skip(1)) {
 			j = getch();
-			true = !ismot(j) && cbits(j) && cbits(j) != ' ';
+			trueflag = !ismot(j) && cbits(j) && cbits(j) != ' ';
 		}
 		tryglf--;
 		warn = w;
@@ -1674,7 +1674,7 @@ caseif(int x)
 		warn &= ~(WARN_MAC|WARN_SPACE|WARN_REG);
 		if (!skip(1)) {
 			j = getrq(2);
-			true = (cbits(i) == 'r' ?
+			trueflag = (cbits(i) == 'r' ?
 					usedr(j) != NULL : findmn(j) != NULL);
 		}
 		warn = w;
@@ -1684,7 +1684,7 @@ caseif(int x)
 			goto dfl;
 		if (!skip(1)) {
 			j = getrq(3);
-			true = findft(j, 0) != -1;
+			trueflag = findft(j, 0) != -1;
 		}
 		break;
 	case 'v':
@@ -1692,14 +1692,14 @@ caseif(int x)
 	case ' ':
 		break;
 	default:
-	dfl:	true = cmpstr(i);
+	dfl:	trueflag = cmpstr(i);
 	}
 i1:
-	true ^= notflag;
+	trueflag ^= notflag;
 	if (x == 1) {
-		el = !true;
+		el = !trueflag;
 	}
-	if (true) {
+	if (trueflag) {
 		if (frame->loopf & LOOP_EVAL) {
 			if (nonumb)
 				goto i3;
