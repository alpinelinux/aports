--- a/main.go
+++ b/main.go
@@ -165,7 +165,7 @@ func redirErr() {
 		return
 	}
 	defer fd.Close()
-	syscall.Dup2(int(fd.Fd()), 2)
+	syscall.Dup3(int(fd.Fd()), 2, 0)
 }
 
 var alreadylogged = make(map[string]bool)
