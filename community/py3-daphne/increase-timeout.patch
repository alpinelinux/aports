diff --git a/daphne/testing.py b/daphne/testing.py
index 40fcedd..371fc5c 100644
--- a/daphne/testing.py
+++ b/daphne/testing.py
@@ -15,7 +15,7 @@ class BaseDaphneTestingInstance:
     Works as a context manager.
     """
 
-    startup_timeout = 2
+    startup_timeout = 10
 
     def __init__(
         self,
diff --git a/tests/http_base.py b/tests/http_base.py
index e5a80c2..73ef024 100644
--- a/tests/http_base.py
+++ b/tests/http_base.py
@@ -27,7 +27,7 @@ class DaphneTestCase(unittest.TestCase):
         body,
         responses,
         headers=None,
-        timeout=1,
+        timeout=20,
         xff=False,
         request_buffer_size=None,
     ):
@@ -67,7 +67,7 @@ class DaphneTestCase(unittest.TestCase):
             # Return scope, messages, response
             return test_app.get_received() + (response,)
 
-    def run_daphne_raw(self, data, *, responses=None, timeout=1):
+    def run_daphne_raw(self, data, *, responses=None, timeout=20):
         """
         Runs Daphne and sends it the given raw bytestring over a socket.
         Accepts list of response messages the application will reply with.
@@ -137,7 +137,7 @@ class DaphneTestCase(unittest.TestCase):
         params=None,
         headers=None,
         subprotocols=None,
-        timeout=1,
+        timeout=20,
     ):
         """
         Runs a WebSocket handshake negotiation and returns the raw socket
@@ -206,7 +206,7 @@ class DaphneTestCase(unittest.TestCase):
         frame += value
         sock.sendall(frame)
 
-    def receive_from_socket(self, sock, length, timeout=1):
+    def receive_from_socket(self, sock, length, timeout=20):
         """
         Receives the given amount of bytes from the socket, or times out.
         """
diff --git a/tests/test_http_request.py b/tests/test_http_request.py
index f0fe376..52bd443 100644
--- a/tests/test_http_request.py
+++ b/tests/test_http_request.py
@@ -121,7 +121,7 @@ class TestHTTPRequest(DaphneTestCase):
         self.assert_valid_http_request_message(messages[0], body=b"")
 
     @given(request_path=http_strategies.http_path(), chunk_size=integers(min_value=1))
-    @settings(max_examples=5, deadline=5000)
+    @settings(max_examples=5, deadline=20000)
     def test_request_body_chunking(self, request_path, chunk_size):
         """
         Tests request body chunking logic.
