Patch-Source: https://github.com/caronc/apprise/issues/XXXX
--
Use isolated tmpdir fixture instead of shared /tmp to avoid picking up
stray .py files left by other processes on the build system.

--- a/tests/test_api.py
+++ b/tests/test_api.py
@@ -1337,8 +1337,8 @@
         == "http://localhost/default/info-256x256.test"
     )

-    a = AppriseAsset(plugin_paths=("/tmp",))
-    assert a.plugin_paths == ("/tmp",)
+    a = AppriseAsset(plugin_paths=(str(tmpdir),))
+    assert a.plugin_paths == (str(tmpdir),)


 def test_apprise_disabled_plugins():
