# Contributor: Anjandev Momi <anjan@momi.ca>
# Maintainer: Adam Thiede <me@adamthiede.com>
pkgname=river
pkgver=0.4.8
pkgrel=0
pkgdesc="Non-monolithic Wayland compositor"
url="https://codeberg.org/river/river"
arch="all !armhf !armv7 !x86" # limited by zig aport
license="GPL-3.0-only"
makedepends="
	libevdev-dev
	libxkbcommon-dev
	pixman-dev
	scdoc
	wayland-dev
	wayland-protocols
	wlroots0.20-dev
	zig
	"
depends="xwayland seatd"
subpackages="
	$pkgname-dev
	$pkgname-doc
	"
options="net"
source="
	https://codeberg.org/river/river/releases/download/v$pkgver/river-$pkgver.tar.gz
	"

_buildzigflags="-Dpie -Dxwayland -Dllvm -Dcpu=baseline"

build() {
	# This installs it to $builddir/out
	DESTDIR="$builddir/out" zig build \
		-Doptimize=ReleaseSafe \
		$_buildzigflags \
		--prefix /usr install
}

check() {
	zig build test $_buildzigflags
}

package() {
	mkdir -p "$pkgdir"
	cp -r out/* "$pkgdir"

	# Fix location of pkgconfig files, must be fixed upstream
	mkdir -p "$pkgdir"/usr/lib
	mv "$pkgdir"/usr/share/pkgconfig "$pkgdir"/usr/lib
}

dev() {
	default_dev
	amove usr/share/river-protocols
}

sha512sums="
a3f4f016f6364be99360f72d848dda7a5825d52d2227f5d8cef65ecb1faf7b691bc099fb9186faf9765080691c57a2eacafb20475e8b90a911fda2e498d91511  river-0.4.8.tar.gz
"
