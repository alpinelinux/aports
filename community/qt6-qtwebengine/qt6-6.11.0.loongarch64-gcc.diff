diff --git a/src/3rdparty/chromium/skia/BUILD.gn b/src/3rdparty/chromium/skia/BUILD.gn
index 48edd7108d..f6e2febed3 100644
--- a/src/3rdparty/chromium/skia/BUILD.gn
+++ b/src/3rdparty/chromium/skia/BUILD.gn
@@ -164,7 +164,7 @@ config("skia_config") {
   if (current_cpu == "loong64") {
     cflags = [
       "-mlsx",
-      "-flax-vector-conversions=all",
+      "-flax-vector-conversions",
     ]
   }
 }
@@ -793,7 +793,7 @@ if (current_cpu == "loong64") {
     sources = skia_opts.lasx_sources
     cflags = [
       "-mlasx",
-      "-flax-vector-conversions=all",
+      "-flax-vector-conversions",
     ]
     visibility = [ ":skia_opts" ]
   }
diff --git a/src/3rdparty/chromium/third_party/blink/renderer/platform/BUILD.gn b/src/3rdparty/chromium/third_party/blink/renderer/platform/BUILD.gn
index 614dfa2bf6..4a6317931e 100644
--- a/src/3rdparty/chromium/third_party/blink/renderer/platform/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/blink/renderer/platform/BUILD.gn
@@ -1684,7 +1684,7 @@ jumbo_component("platform") {
   if (current_cpu == "loong64") {
     cflags = [
       "-mlsx",
-      "-flax-vector-conversions=all",
+      "-flax-vector-conversions",
     ]
   }
 
diff --git a/src/3rdparty/chromium/third_party/skia/src/base/SkUtils.h b/src/3rdparty/chromium/third_party/skia/src/base/SkUtils.h
index 5a4759927a..a055971fe2 100644
--- a/src/3rdparty/chromium/third_party/skia/src/base/SkUtils.h
+++ b/src/3rdparty/chromium/third_party/skia/src/base/SkUtils.h
@@ -53,14 +53,14 @@ static SK_ALWAYS_INLINE T SK_FP_SAFE_ABI sk_unaligned_load(const P* ptr) {
     //    non-trivial).
     // Use `reinterpret_cast<const void*>` to explicit suppress this warning; a
     // trivially copyable type is safe to memcpy from/to.
-    memcpy(&val, static_cast<const void*>(ptr), sizeof(val));
+    __builtin_memcpy(&val, static_cast<const void*>(ptr), sizeof(val));
     return val;
 }
 
 template <typename T, typename P>
 static SK_ALWAYS_INLINE void SK_FP_SAFE_ABI sk_unaligned_store(P* ptr, T val) {
     static_assert(std::is_trivially_copyable<T>::value);
-    memcpy(ptr, &val, sizeof(val));
+    __builtin_memcpy(ptr, &val, sizeof(val));
 }
 
 // Copy the bytes from src into an instance of type Dst and return it.
diff --git a/src/3rdparty/chromium/third_party/skia/src/base/SkVx.h b/src/3rdparty/chromium/third_party/skia/src/base/SkVx.h
index 0c77e4a1b2..f544a2623c 100644
--- a/src/3rdparty/chromium/third_party/skia/src/base/SkVx.h
+++ b/src/3rdparty/chromium/third_party/skia/src/base/SkVx.h
@@ -104,7 +104,7 @@ struct alignas(N*sizeof(T)) Vec {
     SKVX_ALWAYS_INLINE Vec(std::initializer_list<T> xs) {
         T vals[N] = {0};
         assert(xs.size() <= (size_t)N);
-        memcpy(vals, xs.begin(), std::min(xs.size(), (size_t)N)*sizeof(T));
+        __builtin_memcpy(vals, xs.begin(), std::min(xs.size(), (size_t)N)*sizeof(T));
 
         this->lo = Vec<N/2,T>::Load(vals +   0);
         this->hi = Vec<N/2,T>::Load(vals + N/2);
@@ -118,7 +118,7 @@ struct alignas(N*sizeof(T)) Vec {
     }
     SKVX_ALWAYS_INLINE void store(void* ptr) const {
         // Note: Calling sk_unaligned_store produces slightly worse code here, for some reason
-        memcpy(ptr, this, sizeof(Vec));
+        __builtin_memcpy(ptr, this, sizeof(Vec));
     }
 
     Vec<N/2,T> lo, hi;
@@ -140,7 +140,7 @@ struct alignas(4*sizeof(T)) Vec<4,T> {
     SKVX_ALWAYS_INLINE Vec(std::initializer_list<T> xs) {
         T vals[4] = {0};
         assert(xs.size() <= (size_t)4);
-        memcpy(vals, xs.begin(), std::min(xs.size(), (size_t)4)*sizeof(T));
+        __builtin_memcpy(vals, xs.begin(), std::min(xs.size(), (size_t)4)*sizeof(T));
 
         this->lo = Vec<2,T>::Load(vals + 0);
         this->hi = Vec<2,T>::Load(vals + 2);
@@ -153,7 +153,7 @@ struct alignas(4*sizeof(T)) Vec<4,T> {
         return sk_unaligned_load<Vec>(ptr);
     }
     SKVX_ALWAYS_INLINE void store(void* ptr) const {
-        memcpy(ptr, this, sizeof(Vec));
+        __builtin_memcpy(ptr, this, sizeof(Vec));
     }
 
     SKVX_ALWAYS_INLINE Vec<2,T>& xy() { return lo; }
@@ -188,7 +188,7 @@ struct alignas(2*sizeof(T)) Vec<2,T> {
     SKVX_ALWAYS_INLINE Vec(std::initializer_list<T> xs) {
         T vals[2] = {0};
         assert(xs.size() <= (size_t)2);
-        memcpy(vals, xs.begin(), std::min(xs.size(), (size_t)2)*sizeof(T));
+        __builtin_memcpy(vals, xs.begin(), std::min(xs.size(), (size_t)2)*sizeof(T));
 
         this->lo = Vec<1,T>::Load(vals + 0);
         this->hi = Vec<1,T>::Load(vals + 1);
@@ -201,7 +201,7 @@ struct alignas(2*sizeof(T)) Vec<2,T> {
         return sk_unaligned_load<Vec>(ptr);
     }
     SKVX_ALWAYS_INLINE void store(void* ptr) const {
-        memcpy(ptr, this, sizeof(Vec));
+        __builtin_memcpy(ptr, this, sizeof(Vec));
     }
 
     SKVX_ALWAYS_INLINE T& x() { return lo.val; }
@@ -235,7 +235,7 @@ struct Vec<1,T> {
         return sk_unaligned_load<Vec>(ptr);
     }
     SKVX_ALWAYS_INLINE void store(void* ptr) const {
-        memcpy(ptr, this, sizeof(Vec));
+        __builtin_memcpy(ptr, this, sizeof(Vec));
     }
 };
 
diff --git a/src/3rdparty/chromium/third_party/skia/src/opts/SkOpts_SetTarget.h b/src/3rdparty/chromium/third_party/skia/src/opts/SkOpts_SetTarget.h
index 48c0b9de83..e5bc993c5a 100644
--- a/src/3rdparty/chromium/third_party/skia/src/opts/SkOpts_SetTarget.h
+++ b/src/3rdparty/chromium/third_party/skia/src/opts/SkOpts_SetTarget.h
@@ -140,6 +140,9 @@
 
         #if defined(__clang__)
           #pragma clang attribute push(__attribute__((target("lsx,lasx"))), apply_to=function)
+        #elif defined(__GNUC__)
+          #pragma GCC push_options
+          #pragma GCC target("lsx,lasx")
         #endif
 
     #else

