diff --git a/src/3rdparty/chromium/sandbox/linux/seccomp-bpf-helpers/sigsys_handlers.cc b/src/3rdparty/chromium/sandbox/linux/seccomp-bpf-helpers/sigsys_handlers.cc
index 6cf19f4514..323c8c2c05 100644
--- a/src/3rdparty/chromium/sandbox/linux/seccomp-bpf-helpers/sigsys_handlers.cc
+++ b/src/3rdparty/chromium/sandbox/linux/seccomp-bpf-helpers/sigsys_handlers.cc
@@ -213,6 +213,7 @@ intptr_t SIGSYSIoctlFailure(const struct arch_seccomp_data& args,
   WriteToStdErr(kSeccompIoctlError, sizeof(kSeccompIoctlError) - 1);
   PrintAndSetSeccompCrashKey(args);

+#if !defined(__loongarch__)
   // Log information about the file descriptor.
   char message[256];
   size_t message_len;
@@ -236,6 +237,7 @@ intptr_t SIGSYSIoctlFailure(const struct arch_seccomp_data& args,
   }
   WriteToStdErr(message, message_len);
   base::debug::SetCrashKeyString(seccomp_ioctl_crash_key, message);
+#endif

   // Make "request" volatile so that we can see it on the stack in a minidump.
   volatile uint64_t request = args.args[1];
