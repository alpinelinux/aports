diff --git a/digitalocean/tests/test_firewall.py b/digitalocean/tests/test_firewall.py
index 439de60..6342ad4 100644
--- a/digitalocean/tests/test_firewall.py
+++ b/digitalocean/tests/test_firewall.py
@@ -64,10 +64,7 @@ class TestFirewall(BaseTest):
         data = self.load_from_file('firewalls/droplets.json')
 
         url = self.base_url + "firewalls/12345/droplets"
-        responses.add(responses.POST, url,
-                      body=data,
-                      status=204,
-                      content_type='application/json')
+        responses.add(responses.POST, url, status=204)
 
         droplet_id = json.loads(data)["droplet_ids"][0]
         self.firewall.add_droplets([droplet_id])
@@ -79,11 +76,7 @@ class TestFirewall(BaseTest):
         data = self.load_from_file('firewalls/droplets.json')
 
         url = self.base_url + "firewalls/12345/droplets"
-        responses.add(responses.DELETE,
-                      url,
-                      body=data,
-                      status=204,
-                      content_type='application/json')
+        responses.add(responses.DELETE, url, status=204)
 
         droplet_id = json.loads(data)["droplet_ids"][0]
         self.firewall.remove_droplets([droplet_id])
@@ -95,10 +88,7 @@ class TestFirewall(BaseTest):
         data = self.load_from_file('firewalls/tags.json')
 
         url = self.base_url + "firewalls/12345/tags"
-        responses.add(responses.POST, url,
-                      body=data,
-                      status=204,
-                      content_type='application/json')
+        responses.add(responses.POST, url, status=204)
 
         tag = json.loads(data)["tags"][0]
         self.firewall.add_tags([tag])
@@ -110,10 +100,7 @@ class TestFirewall(BaseTest):
         data = self.load_from_file('firewalls/tags.json')
 
         url = self.base_url + "firewalls/12345/tags"
-        responses.add(responses.DELETE, url,
-                      body=data,
-                      status=204,
-                      content_type='application/json')
+        responses.add(responses.DELETE, url, status=204)
 
         tag = json.loads(data)["tags"][0]
         self.firewall.remove_tags([tag])
