diff --git a/src/bumprace.c b/src/bumprace.c
index 6831f5a..5574f25 100644
--- a/src/bumprace.c
+++ b/src/bumprace.c
@@ -1178,7 +1178,7 @@ int main(int argc, char *argv[])
   sprintf(text,"%s/font.scl",DATAPATH);
   Font=LoadImage("font.png",3);
   InitFont(Font);
-  if (final) StartText(i);
+  if (final) StartText();
 //load data
   printf("** Loading Data **\n");
 #ifdef SOUND
