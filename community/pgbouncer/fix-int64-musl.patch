diff --git a/include/common/simd.h b/include/common/simd.h
index 97c5f35..beb1458 100644
--- a/include/common/simd.h
+++ b/include/common/simd.h
@@ -236,7 +236,7 @@ vector8_has_le(const Vector8 v, const uint8 c)
 	 * Adapted from
 	 * https://graphics.stanford.edu/~seander/bithacks.html#HasLessInWord
 	 */
-	if ((int64) v >= 0 && c < 0x80)
+	if ((int64_t) v >= 0 && c < 0x80)
 		result = (v - vector8_broadcast(c + 1)) & ~v & vector8_broadcast(0x80);
 	else
 	{
