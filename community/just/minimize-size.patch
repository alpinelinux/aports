Minimize the binary size.

--- a/Cargo.toml
+++ b/Cargo.toml
@@ -66,6 +66,9 @@ targets = ["x86_64-unknown-linux-gnu"]
 
 [profile.release]
 lto = true
+codegen-units = 1
+opt-level = "z"
+panic = "abort"
 
 [[test]]
 name = "integration"
