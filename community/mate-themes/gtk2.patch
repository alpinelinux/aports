Ignore failure to find gtk 2

--- a/configure
+++ b/configure
@@ -7192,7 +7192,7 @@
   pkg_cv_THEME_ENGINE_CFLAGS=`$PKG_CONFIG --cflags "gtk+-2.0 >= 2.0.0   gdk-pixbuf-2.0 >= 2.0.0 " 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
-  pkg_failed=yes
+  pkg_failed=no
 fi
  else
     pkg_failed=untried
@@ -7209,7 +7209,7 @@
   pkg_cv_THEME_ENGINE_LIBS=`$PKG_CONFIG --libs "gtk+-2.0 >= 2.0.0   gdk-pixbuf-2.0 >= 2.0.0 " 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
-  pkg_failed=yes
+  pkg_failed=no
 fi
  else
     pkg_failed=untried
