diff --git a/CMakeLists.txt b/CMakeLists.txt
index 11e429677..3e1e21d0d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1976,7 +1976,7 @@ add_subdirectory(libs/nmea0183)
 add_subdirectory(libs/observable)
 add_subdirectory(libs/picosha2)
 add_subdirectory(libs/pugixml)
-add_subdirectory(libs/rapidjson)
+find_package(RapidJSON)
 add_subdirectory(libs/s52plib)
 add_subdirectory(libs/sqlite)
 
diff --git a/model/CMakeLists.txt b/model/CMakeLists.txt
index 77ee8748a..931679d14 100644
--- a/model/CMakeLists.txt
+++ b/model/CMakeLists.txt
@@ -477,7 +477,6 @@ target_link_libraries(
     ocpn::N2KParser
     ocpn::nmea0183
     ocpn::pugixml
-    ocpn::rapidjson
     ocpn::sqlite
     ocpn::s52plib
     ocpn::sound
@@ -487,6 +486,9 @@ target_link_libraries(
     pico_sha2
 )
 
+find_package(RapidJSON REQUIRED)
+target_include_directories(_OCPN_MODEL INTERFACE ${RapidJSON_INCLUDE_DIRS})
+
 if (OCPN_USE_NEWSERIAL)
   target_link_libraries(_OCPN_MODEL INTERFACE ocpn::serial)
 endif ()
diff --git a/model/src/ais_decoder.cpp b/model/src/ais_decoder.cpp
index a38c086d0..a09e9b8f3 100644
--- a/model/src/ais_decoder.cpp
+++ b/model/src/ais_decoder.cpp
@@ -1037,7 +1037,7 @@ void AisDecoder::BuildERIShipTypeHash(void) {
 void AisDecoder::HandleSignalK(std::shared_ptr<const SignalkMsg> sK_msg) {
   rapidjson::Document root;
 
-  root.Parse(sK_msg->raw_message);
+  root.Parse(sK_msg->raw_message.c_str(), sK_msg->raw_message.size());
 
   if (root.HasParseError()) return;
 
diff --git a/model/src/comm_decoder.cpp b/model/src/comm_decoder.cpp
index 262fe0a8b..59135ef51 100644
--- a/model/src/comm_decoder.cpp
+++ b/model/src/comm_decoder.cpp
@@ -379,7 +379,7 @@ bool CommDecoder::DecodePGN129540(std::vector<unsigned char> v,
 bool CommDecoder::DecodeSignalK(std::string s, NavData& temp_data) {
   rapidjson::Document root;
 
-  root.Parse(s);
+  root.Parse(s.c_str(), s.size());
   if (root.HasParseError()) return false;
 
   if (root.HasMember("updates") && root["updates"].IsArray()) {
diff --git a/model/src/comm_drv_signalk_net.cpp b/model/src/comm_drv_signalk_net.cpp
index 995a3fe35..990b929cd 100644
--- a/model/src/comm_drv_signalk_net.cpp
+++ b/model/src/comm_drv_signalk_net.cpp
@@ -435,7 +435,7 @@ void CommDriverSignalKNet::handle_SK_sentence(
   std::string msgTerminated = *msg;
   msgTerminated.append("\r\n");
 
-  root.Parse(*msg);
+  root.Parse(msg->c_str(), msg->size());
   if (root.HasParseError()) {
     wxLogMessage(wxString::Format(
         _T("SignalKDataStream ERROR: the JSON document is not well-formed:%d"),
