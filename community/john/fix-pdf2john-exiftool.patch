diff --git a/run/pdf2john.pl b/run/pdf2john.pl
index 383ab31..7e412f0 100755
--- a/run/pdf2john.pl
+++ b/run/pdf2john.pl
@@ -17,7 +17,7 @@
 #
 # "pdf2john.pl" was glued together by Dhiru Kholia.
 
-use warnings;
+no warnings 'once';
 use strict;
 require 5.004;
 
@@ -36,7 +36,7 @@ BEGIN {
         $ExifTool::configFile = shift;
     }
 }
-use ExifTool qw{:Public Open};
+use Image::ExifTool;
 
 # function prototypes
 sub SigInt();
@@ -400,7 +400,7 @@ if ($^O eq 'MSWin32' and eval 'require File::Glob') {
     $doGlob = 1;
 }
 
-$mainTool = new ExifTool;        # create ExifTool object
+$mainTool = Image::ExifTool->new();	# create ExifTool object
 
 # don't extract duplicates by default unless set by UserDefined::Options
 $mainTool->Options(Duplicates => 0) unless %ExifTool::UserDefined::Options
