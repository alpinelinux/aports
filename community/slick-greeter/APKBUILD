# Contributor: David Demelier <markand@malikania.fr>
maintainer="Jakko <jahau@rocketmail.com>"
pkgname=slick-greeter
pkgver=2.2.5
pkgrel=0
pkgdesc="a slick-looking LightDM greeter"
url="https://github.com/linuxmint/slick-greeter"
# s390x: mainframe
arch="all !s390x"
license="GPL-3.0-only"
makedepends="libcanberra-dev lightdm-dev meson vala xapp-dev"
subpackages="$pkgname-doc $pkgname-lang"
source="$pkgname-$pkgver.tar.gz::https://github.com/linuxmint/slick-greeter/archive/refs/tags/$pkgver.tar.gz"

build() {
	abuild-meson \
		. output
	meson compile -C output
}

check() {
	meson test --print-errorlogs -C output
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

sha512sums="
799a4420ad155c3d59c5a0129ddcd2cdef3e3eef0eb151ab166b20818839be0889bdb739b55c4c2e9a61b60bd73ab9bbad8efd5459e2e44f71259bab039a66ac  slick-greeter-2.2.5.tar.gz
"
