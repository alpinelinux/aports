--- a/src/test.rs
+++ b/src/test.rs
@@ -137,6 +137,9 @@ fn tempdir_in_with_prefix<P: AsRef<Path>>(path: P, prefix: &str) -> io::Result<P
 /// this_host_triple that doesn't make its own process or use
 /// TargetTriple::from_host() from within the process context as needed.
 pub fn this_host_triple() -> String {
+    if cfg!(target_env = "musl") {
+        return TargetTriple::from_build().to_string();
+    }
     if cfg!(target_os = "windows") {
         // For windows, this host may be different to the target: we may be
         // building with i686 toolchain, but on an x86_64 host, so run the
@@ -319,7 +322,7 @@ pub fn create_hash(src: &Path, dst: &Path) -> String {
     hex
 }
 
-pub static CROSS_ARCH1: &str = "x86_64-unknown-linux-musl";
+pub static CROSS_ARCH1: &str = "x86_64-unknown-linux-gnu";
 pub static CROSS_ARCH2: &str = "arm-linux-androideabi";
 
 // Architecture for testing 'multi-host' installation.
--- a/tests/suite/cli_rustup.rs
+++ b/tests/suite/cli_rustup.rs
@@ -3459,7 +3459,7 @@ async fn file_override_with_target_info() {
     cx.config
         .expect(["rustc", "--version"])
         .await
-        .remove_redactions(["[HOST_TRIPLE]"])
+        .remove_redactions(["[HOST_TRIPLE]", "[CROSS_ARCH_I]"])
         .with_stderr(snapbox::str![[r#"
 ...
 error: target tuple in channel name 'nightly-x86_64-unknown-linux-gnu'
