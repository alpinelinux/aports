#!/bin/sh

cat >&2 <<-EOF
glycin loaders got split into their individual subpackages.
By default, only glycin-image-rs & glycin-svg are installed.
Additional loader subpackages are glycin-heif & glycin-jxl.

To install all available loaders, install glycin-loaders-all.

Also the glycin-thumbnailer got subpackaged and isn't installed by default.
EOF
