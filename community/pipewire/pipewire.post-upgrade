#!/bin/sh

ver_old="$2"

if [ "$(apk version -t "$ver_old" '0.3.33-r3')" = '<' ]; then
	cat >&2 <<-EOF
	*
	* Pipewire Bluetooth support (bluez5 module) has been moved to a subpackage.
	* If you use it, install: pipewire-spa-bluez.
	*
	EOF
fi
