Need rust nightly and webdriver

--- a/crates/cli/tests/wasm-bindgen-test-runner/main.rs
+++ b/crates/cli/tests/wasm-bindgen-test-runner/main.rs
@@ -140,6 +140,7 @@ fn test_wasm_bindgen_test_runner_list() {
 /// Test that console.log output in dedicated worker mode is not duplicated.
 /// See: https://github.com/wasm-bindgen/wasm-bindgen/pull/4845#issuecomment-3660688206
 #[test]
+#[cfg(not(target_env = "musl"))]
 fn test_worker_console_log_no_duplicates() {
     let output = Project::new("test_worker_console_log_no_duplicates")
         .file(
--- a/crates/cli/tests/wasm-bindgen/reference.rs
+++ b/crates/cli/tests/wasm-bindgen/reference.rs
@@ -134,6 +134,7 @@ fn runtest(
 }
 
 #[test]
+#[cfg(not(target_env = "musl"))]
 fn runtest_targets_atomics() -> Result<()> {
     let mut test = PathBuf::from(env!("CARGO_MANIFEST_DIR"));
     test.push("tests");
@@ -159,6 +160,7 @@ fn runtest_targets_atomics() -> Result<()> {
 }
 
 #[test]
+#[cfg(not(target_env = "musl"))]
 fn runtest_targets_mvp() -> Result<()> {
     let mut test = PathBuf::from(env!("CARGO_MANIFEST_DIR"));
     test.push("tests");
