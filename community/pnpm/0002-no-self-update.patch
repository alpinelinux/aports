Disable command "pnpm self-update" as we install via apk

--- a/dist/pnpm.mjs
+++ b/dist/pnpm.mjs
@@ -207103,6 +207103,7 @@
   });
 }
 async function handler33(opts2, params) {
+  throw new error_1.PnpmError("CANT_SELF_UPDATE_IN_APK", "You should update pnpm with apk");
   if (isExecutedByCorepack()) {
     throw new PnpmError("CANT_SELF_UPDATE_IN_COREPACK", "You should update pnpm with corepack");
   }
