# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-settings-daemon
pkgver=1.0.11
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Settings daemon for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-settings-daemon"
# riscv64: too slow to build
arch="all !riscv64"
license="GPL-3.0-only"
depends="
	acpid
	alsa-utils
	geoclue
	playerctl
	pulseaudio-dev
"
makedepends="
	cargo-auditable
	libinput-dev
	openssl-dev
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-settings-daemon/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CHOST" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	make DESTDIR="$pkgdir" install
}

sha512sums="
b38f774013dd0bdf51ed19352c77599e8c54fb243e4c9634a972df3c9cc87116dd88590cc62a4aba879d809ed3c9406d162a2f704590099e90d6a571e8da2858  cosmic-settings-daemon-1.0.11.tar.gz
"
