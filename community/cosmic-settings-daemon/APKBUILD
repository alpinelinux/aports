# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-settings-daemon
pkgver=1.6.0
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Settings daemon for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-settings-daemon"
# armv7, armhf, x86: DO NOT RE-ENABLE! 32-bit is problematic, often requires carrying patches to fix
# riscv64: too slow to build
arch="all !armv7 !armhf !riscv64 !x86"
license="GPL-3.0-only"
depends="
	acpid
	alsa-utils
	geoclue
	playerctl
"
makedepends="
	cargo-auditable
	clang22-libclang
	libinput-dev
	libxkbcommon-dev
	openssl-dev
	pipewire-dev
	pulseaudio-dev
"
options="net !check"  # fetch dependencies, no tests
source="
	$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-settings-daemon/archive/refs/tags/epoch-$_upstreamver.tar.gz
"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CHOST" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	make DESTDIR="$pkgdir" install
}

sha512sums="
853aaf6628c5301eb37a54ac432936dab7d0c64f41b6c566b0b4a40bcb761f79e301ce455594f71d6ad604e3c9d3b6d922783b99c3446de0609758288ae7f877  cosmic-settings-daemon-1.6.0.tar.gz
"
