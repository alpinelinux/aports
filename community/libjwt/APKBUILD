# Maintainer: Jakub Jirutka <jakub@jirutka.cz>
pkgname=libjwt
pkgver=3.6.1
pkgrel=0
pkgdesc="The C JSON Web Token Library +JWE +JWK +JWKS"
url="https://github.com/benmcollins/libjwt"
arch="all"
license="MPL-2.0"
makedepends="
	cmake
	jansson-dev
	openssl-dev
	samurai
	"
checkdepends="
	bash
	check-dev
	"
subpackages="
	$pkgname-static
	$pkgname-dev
	$pkgname-doc
	$pkgname-tools
	"
source="$pkgname-$pkgver.tar.gz::https://github.com/benmcollins/libjwt/archive/refs/tags/v$pkgver.tar.gz"

build() {
	local crossopts=
	[ "$CBUILD" != "$CHOST" ] \
		&& crossopts="-DCMAKE_SYSTEM_NAME=Linux -DCMAKE_HOST_SYSTEM_NAME=Linux"

	cmake -B build -G Ninja \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_INSTALL_LIBDIR=lib \
		-DBUILD_SHARED_LIBS=ON \
		-DCMAKE_BUILD_TYPE=None \
		-DWITH_GNUTLS=OFF \
		-DWITH_MBEDTLS=OFF \
		-DWITH_LIBCURL=OFF \
		-DWITH_TESTS="$(want_check && echo ON || echo OFF)" \
		$crossopts
	cmake --build build
}

check() {
	ctest --test-dir build
}

package() {
	DESTDIR="$pkgdir" cmake --install build
}

tools() {
	pkgdesc="Tools for JWE, JWT and JWK(S) from libjwt"

	amove usr/bin
}

sha512sums="
736e71dbbb3f9ae81c9106ced3dc522bc64dc9ec9ffbc52fe286acada3b8fe8ab1b2fc8c689b79857e6c8a0d66a12643443a3e2ef00657061f637da50cf8c085  libjwt-3.6.1.tar.gz
"
