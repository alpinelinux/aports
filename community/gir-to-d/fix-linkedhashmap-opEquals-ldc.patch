diff --git a/source/gtd/LinkedHasMap.d b/source/gtd/LinkedHasMap.d
--- a/source/gtd/LinkedHasMap.d
+++ b/source/gtd/LinkedHasMap.d
@@ -222,9 +222,10 @@ struct LinkedHashMap(Key, Value)
 	/**
 	 * Returns: true if this and that are equal.
 	 */
-	bool opEquals(inout typeof(this) that) inout nothrow pure @safe
+	bool opEquals(inout typeof(this) that) inout nothrow pure @trusted
 	{
-		return data == that.data;
+		// Avoid AA equality on inout(Node)* values (rejected by modern druntime).
+		return (cast(const(Node*[Key]))data) == (cast(const(Node*[Key]))that.data);
 	}
 
 	/**
