# Maintainer: omni <omni+alpine@hack.org>
maintainer="omni <omni+alpine@hack.org>"
pkgname=mangowm
pkgver=0.14.2
pkgrel=0
pkgdesc="Lightweight, high-performance Wayland compositor built on dwl"
arch="all"
url="https://mangowm.github.io/"
license="GPL-3.0-or-later"
makedepends="muon
	cjson-dev
	pcre2-dev
	scenefx-dev
	wlroots0.19-dev
	wayland-protocols
	"
subpackages="$pkgname-doc"
source="$pkgname-$pkgver.tar.gz::https://github.com/mangowm/mango/archive/refs/tags/$pkgver.tar.gz"
builddir="$srcdir/mango-$pkgver"

build() {
	abuild-muon output .
	ninja -C output
}

check() {
	./output/mango -v
}

package() {
	DESTDIR="$pkgdir" muon -C output install
	provides="mangowc=$pkgver-r$pkgrel"

	install -dm755 "$pkgdir"/usr/share/doc/"$pkgname"/
	mv docs/* "$pkgdir"/usr/share/doc/"$pkgname"/
}

sha512sums="
1638dd2b1237c3e0afb86b80b2b613cc29e10f7c0b468b82871e570ad0a4825fe4417897b46f21f0d5dd7d9e84afc8579aad89ffafad1c4a93187906b8bf592b  mangowm-0.14.2.tar.gz
"
