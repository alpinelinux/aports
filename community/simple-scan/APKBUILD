# Contributor: Rasmus Thomsen <oss@cogitri.dev>
# Contributor: Leonardo Arena <rnalrd@alpinelinux.org>
# Maintainer: team/gnome <achill@achill.org>
pkgname=simple-scan
pkgver=49.1
pkgrel=1
pkgdesc="Simple Scan allows you to get images from a scanner"
url="https://gitlab.gnome.org/GNOME/simple-scan"
arch="all"
license="GPL-3.0-or-later"
makedepends="
	colord-dev
	dconf-dev
	gtk4.0-dev
	itstool
	libadwaita-dev
	libgusb-dev
	libwebp-dev
	libxml2-utils
	meson
	sane-dev
	vala
	"
subpackages="$pkgname-doc $pkgname-lang"
# FIXME: The normal tarball upload seems to be missing, so for now let's grab it from gitlab
# source="https://download.gnome.org/sources/simple-scan/${pkgver%.*}/simple-scan-$pkgver.tar.xz"
source="https://gitlab.gnome.org/GNOME/simple-scan/-/archive/$pkgver/simple-scan-$pkgver.tar.gz"

build() {
	abuild-meson -Db_lto=true . output
	meson compile -C output
}

check() {
	meson test --print-errorlogs -C output

}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

sha512sums="
e80c459798d2fc2c69858cddee779bd657bb13133e3623e3a9e8d275b03b17642b81fb8fe1cf8f86ba7a304917cde2c456c33c3f99bb61b73dcb8f27b453e389  simple-scan-49.1.tar.gz
"
