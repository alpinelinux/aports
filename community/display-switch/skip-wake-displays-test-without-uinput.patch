--- a/src/platform/wake_displays.rs
+++ b/src/platform/wake_displays.rs
@@ -74,8 +74,9 @@
     fn test_wake_displays() {
         let waked = wake_displays();
         if let Err(err) = &waked {
+            let msg = err.to_string();
             assert!(
-                err.to_string() == "Permission denied",
+                msg == "Permission denied" || msg.to_ascii_lowercase().contains("not found"),
                 "Couldn't wake displays: {:?}",
                 err
             );
