# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-panel
pkgver=1.0.0_beta9
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Panel component for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-panel"
# s390x: nix
# riscv64: too slow to build
arch="all !s390x !riscv64"
license="GPL-3.0-or-later"
makedepends="
	appstream-glib
	cargo
	desktop-file-utils
	findutils
	intltool
	just
	libxkbcommon-dev
	wayland-dev
	wayland-protocols
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-panel/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo build --release --frozen
}

check() {
	cargo test --release --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
eb57374fcec8c5553cce7c186ec6efc0f8f9d2e5ae5bdd0970b65fc0398b0c27b2680ac9c7e445b94c081a117c6a4f30949ebee77d6bd3582831202e5053ea7c  cosmic-panel-1.0.0_beta9.tar.gz
"
