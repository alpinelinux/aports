maintainer="Achill Gilgenast <achill@achill.org>"
pkgname=sdl2-compat
pkgver=2.32.66
pkgrel=1
pkgdesc="SDL2 compatibility layer that uses SDL3 behind the scenes"
url="https://github.com/libsdl-org/sdl2-compat"
arch="all"
license="Zlib"
depends="so:libSDL3.so.0"
makedepends="
	cmake
	samurai
	sdl3-dev
	"
subpackages="$pkgname-dev $pkgname-doc"
source="https://github.com/libsdl-org/sdl2-compat/releases/download/release-$pkgver/sdl2-compat-$pkgver.tar.gz"
options="!check" # flaky

build() {
	if [ "$CBUILD" != "$CHOST" ]; then
		local crossopts="-DCMAKE_SYSTEM_NAME=Linux -DCMAKE_HOST_SYSTEM_NAME=Linux"
	fi
	cmake -B build -G Ninja \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_INSTALL_LIBDIR=lib \
		-DBUILD_SHARED_LIBS=ON \
		-DCMAKE_BUILD_TYPE=None \
		$crossopts
	cmake --build build
}

check() {
	ctest --test-dir build
}

package() {
	DESTDIR="$pkgdir" cmake --install build
}

sha512sums="
c8d4d45b9bf2f3904aee19b75878deebecef3b2e68dc2905c787941ad8078753d0616685c5cad586fe823111b026a6b7779faeedcf6daa3ab0a89e48ce3f599f  sdl2-compat-2.32.66.tar.gz
"
