From eb27cf084a90e545c10f8a5435d7f17cf1d28ff9 Mon Sep 17 00:00:00 2001
From: Theelx <43764914+Theelx@users.noreply.github.com>
Date: Wed, 21 Jan 2026 17:01:24 -0500
Subject: [PATCH] Fix pandas 3.0.0 errors and numpy deprecations

Assisted-by: GPT-5.2-Codex via OpenAI-Codex
---
 .pre-commit-config.yaml  |  8 ++++----
 CHANGES.rst              |  2 ++
 jsonpickle/ext/numpy.py  | 10 ++++++----
 jsonpickle/ext/pandas.py |  2 +-
 jsonpickle/tags_pd.py    |  8 ++++++++
 pyproject.toml           |  1 +
 pytest.ini               |  2 +-
 tests/numpy_test.py      | 23 +++++++++++++----------
 tests/pandas_test.py     |  5 +++++
 9 files changed, 41 insertions(+), 20 deletions(-)

diff --git a/jsonpickle/ext/pandas.py b/jsonpickle/ext/pandas.py
index 12317c81..17d4e185 100644
--- a/jsonpickle/ext/pandas.py
+++ b/jsonpickle/ext/pandas.py
@@ -292,7 +292,7 @@ def restore(self, obj: Dict[str, Any]) -> pd.DataFrame:
         for col in df.columns:
             dtype_str = dtypes.get(col, "object")
             try:
-                dtype = np.dtype(dtype_str)
+                dtype = pd.api.types.pandas_dtype(dtype_str)
                 df[col] = df[col].astype(dtype)
             except Exception:
                 msg = (
diff --git a/jsonpickle/tags_pd.py b/jsonpickle/tags_pd.py
index dd426be3..1cdfbb59 100644
--- a/jsonpickle/tags_pd.py
+++ b/jsonpickle/tags_pd.py
@@ -219,6 +219,14 @@ def get_all_pandas_dtype_strings() -> List[DTypeRepr]:
     # use the class object for things that np.dtype can't reconstruct
     dtypes.extend([pd.Timestamp, pd.Timedelta, pd.Period, pd.Interval])
 
+    # include parameterized extension dtypes with storage-specific names
+    # this is required for pandas 3.0.0
+    for storage in ["python", "pyarrow"]:
+        try:
+            dtypes.append(str(pd.StringDtype(storage=storage)).lower())  # type: ignore[call-overload]
+        except Exception:
+            pass
+
     return list(dict.fromkeys(dtypes))
 
 
diff --git a/tests/pandas_test.py b/tests/pandas_test.py
index f5fbef04..0dc19205 100644
--- a/tests/pandas_test.py
+++ b/tests/pandas_test.py
@@ -1,6 +1,7 @@
 import datetime
 
 import pytest
+from packaging.version import parse
 
 try:
     import numpy as np
@@ -331,6 +332,10 @@ def test_multilevel_columns():
     assert names == cloned_data_frame.columns.names
 
 
+@pytest.mark.skipif(
+    parse(pd.__version__) >= parse("3.0.0"),
+    reason="pandas >=3.0.0 isn't supported before jsonpickle 3.4",
+)
 def test_pre_v3_4_df_decoding():
     df = pd.DataFrame(
         {
