Minimize size of the resulting binary: 21572 -> 17616 kiB.
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -62,6 +62,8 @@
 lto = true
 codegen-units = 1
 strip = true
+opt-level = "z"
+panic = "abort"

 [profile.dev]
 # Disabling debug info speeds up builds a bunch,
