# Configuration for /etc/init.d/cortex-tenant

# User (and group) to run cortex-tenant as.
#command_user="cortex-tenant"

# Additional arguments for cortex-tenant daemon.
#command_args=

# Comment out to disable process supervisor.
supervisor=supervise-daemon
