see the following bugs:
- https://bugzilla.mozilla.org/show_bug.cgi?id=2047256
- https://bugzilla.mozilla.org/show_bug.cgi?id=2047651

diff --git a/security/moz.build b/security/moz.build
--- a/security/moz.build
+++ b/security/moz.build
@@ -20,10 +20,14 @@
     BUG_COMPONENT = ("NSS", "Libraries")
 
 if CONFIG["MOZ_SYSTEM_NSS"]:
     Library("nss")
     OS_LIBS += CONFIG["NSS_LIBS"]
+    if CONFIG["OS_TARGET"] == "Linux":
+        OS_LIBS += ["-lfreeblpriv3"]
+    else:
+        OS_LIBS += ["-lfreebl3"]
 
 include("/build/gyp_base.mozbuild")
 if CONFIG["MOZ_FOLD_LIBS"]:
     GeckoSharedLibrary("nss")
     # TODO: The library name can be changed when bug 845217 is fixed.

