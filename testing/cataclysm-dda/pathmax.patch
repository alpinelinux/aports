diff --git a/src/third-party/zstd/common/zstd_internal.h b/src/third-party/zstd/common/zstd_internal.h
index 2789a35..346632b 100644
--- a/src/third-party/zstd/common/zstd_internal.h
+++ b/src/third-party/zstd/common/zstd_internal.h
@@ -54,6 +54,7 @@
 #define MIN(a,b) ((a)<(b) ? (a) : (b))
 #define MAX(a,b) ((a)>(b) ? (a) : (b))
 #define BOUNDED(min,val,max) (MAX(min,MIN(val,max)))
+#define PATH_MAX 4096
 
 
 /*-*************************************
