#!/bin/sh

# Create nmqtt user if it doesn't exist
if ! getent passwd nmqtt >/dev/null; then
	adduser -S -D -H -h /var/lib/nmqtt -s /sbin/nologin -g nmqtt nmqtt 2>/dev/null
fi

if ! getent group nmqtt >/dev/null; then
	addgroup -S nmqtt 2>/dev/null
fi

exit 0
