--- a/drbd/Kbuild
+++ b/drbd/Kbuild
@@ -131,4 +131,4 @@
 		exit 1;								\
 	fi ;									\
 	echo -e "\t\t\" build by $$USER@$$HOSTNAME, `date "+%F %T"`\";\n}";	\
-	mv --force $@.new $@
+	mv -f $@.new $@
