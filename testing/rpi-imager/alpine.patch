diff --git a/src/cmake/GenerateVersion.cmake b/src/cmake/GenerateVersion.cmake
index 2c1eef45..90e68322 100644
--- a/src/cmake/GenerateVersion.cmake
+++ b/src/cmake/GenerateVersion.cmake
@@ -8,21 +8,7 @@
 
 find_package(Git QUIET)
 
-set(VERSION_STR "0.0.0-unknown")
-
-if(GIT_EXECUTABLE)
-    execute_process(
-        COMMAND "${GIT_EXECUTABLE}" describe --tags --always --dirty
-        WORKING_DIRECTORY "${SOURCE_DIR}"
-        OUTPUT_VARIABLE GIT_DESCRIBE
-        OUTPUT_STRIP_TRAILING_WHITESPACE
-        ERROR_QUIET
-        RESULT_VARIABLE GIT_RESULT
-    )
-    if(GIT_RESULT EQUAL 0 AND GIT_DESCRIBE)
-        set(VERSION_STR "${GIT_DESCRIBE}")
-    endif()
-endif()
+set(VERSION_STR "$ENV{VERSION_STR}")
 
 # Parse numeric version components (e.g. v2.0.0-rc4-60-geac7c2f0 → 2, 0, 0, 0)
 #
diff --git a/src/linux/PlatformPackaging.cmake b/src/linux/PlatformPackaging.cmake
index 971842fc..41866ec0 100644
--- a/src/linux/PlatformPackaging.cmake
+++ b/src/linux/PlatformPackaging.cmake
@@ -1,6 +1,6 @@
 # Linux packaging: installation and runtime checks
 
-if (NOT CMAKE_CROSSCOMPILING)
+if (0)
     find_program(LSBLK "lsblk")
     if (NOT LSBLK)
         message(FATAL_ERROR "Unable to locate lsblk (used for disk enumeration)")
diff --git a/src/linux/platformquirks_linux.cpp b/src/linux/platformquirks_linux.cpp
index 9960dc53..14e769a1 100644
--- a/src/linux/platformquirks_linux.cpp
+++ b/src/linux/platformquirks_linux.cpp
@@ -87,7 +87,7 @@ namespace {
         char buf[4096];
         struct iovec iov = { buf, sizeof(buf) };
         struct sockaddr_nl sa;
-        struct msghdr msg = { &sa, sizeof(sa), &iov, 1, nullptr, 0, 0 };
+        struct msghdr msg = { .msg_name = &sa, .msg_namelen = sizeof(sa), .msg_iov = &iov, .msg_iovlen = 1, .msg_control = nullptr, .msg_controllen = 0, .msg_flags = 0 };
         
         // Use poll() instead of select() - no fd number limitations
         struct pollfd fds[2];
