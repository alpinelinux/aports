(lambda (list)
  (flet ((enable  (x) (pushnew x list))
         (disable (x) (setf list (remove x list))))
    (disable :sb-thread)
    (disable :sb-test)
    (enable :sb-unicode)
    (enable :sb-core-compression))
  list)
