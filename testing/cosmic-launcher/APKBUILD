# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-launcher
pkgver=1.0.0_beta1_p1
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Application launcher for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-launcher"
arch="all"
license="GPL-3.0-or-later"
depends="
	pop-launcher
"
makedepends="
	appstream-glib
	cargo
	desktop-file-utils
	intltool
	just
	libxkbcommon-dev
	wayland-dev
	wayland-protocols
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-launcher/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo build --release --frozen
}

check() {
	cargo test --release --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
8a19ee5a1e2e71a27e784fb4fc7fb0384f6b8269e11fdfb8fedce81d5357553d810f3add280d9965086e47d42224e44307f870d6eede909906bbcbe28ba1f39b  cosmic-launcher-1.0.0_beta1_p1.tar.gz
"
