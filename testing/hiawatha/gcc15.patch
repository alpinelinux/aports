--- a/src/hiawatha.c
+++ b/src/hiawatha.c
@@ -317,7 +317,8 @@ void task_runner(t_config *config) {
 
 /* Signal handlers
  */
-void SEGV_handler() {
+void SEGV_handler(int sig) {
+	(void)sig;
 	syslog(LOG_DAEMON | LOG_ALERT, "segmentation fault!");
 #ifdef ENABLE_MONITOR
 	monitor_event("Server crash!");
