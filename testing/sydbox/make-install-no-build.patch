we only use this as an installer helper, in package(), we do the build
ourselves in build() and also don't output to a target specific folder

--- a/Makefile
+++ b/Makefile
@@ -294,10 +294,10 @@ install32: build32
 	for program in $(PROGRAMS); do \
 		$(INSTALL) -pm 0755 target/i686-unknown-linux-musl/release/$$program $(DESTDIR)$(PREFIX)/$(BINDIR)/$$program"32"; \
 	done
-install: release
+install:
 	$(INSTALL) -d $(DESTDIR)$(PREFIX)/$(BINDIR)/
 	for program in $(PROGRAMS); do \
-		$(INSTALL) -pm 0755 target/$(TARGET)/release/$$program $(DESTDIR)$(PREFIX)/$(BINDIR)/; \
+		$(INSTALL) -pm 0755 target/release/$$program $(DESTDIR)$(PREFIX)/$(BINDIR)/; \
 	done
 	$(MAKE) install-man
 	$(MAKE) install-vim
