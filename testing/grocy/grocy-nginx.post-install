#!/bin/ash
#Correct ownership of webapp directories
user=nginx
webapp_root=/usr/share/webapps
webapp_lib=/var/lib/webapps/

for directory in $webapp_root $webapp_root/grocy $webapp_root/grocy/* $webapp_lib $webapp_lib/grocy $webapp_lib/grocy/*; do
	/bin/chown -R $user:$user $directory
done

exit 0
