--- a/src/VBox/Runtime/include/internal/fs.h
+++ b/src/VBox/Runtime/include/internal/fs.h
@@ -59,8 +59,6 @@ int     rtNtQueryFsType(HANDLE hHandle,
 #ifdef RT_OS_LINUX
 # ifdef __USE_MISC
 #  define HAVE_STAT_TIMESPEC_BRIEF
-# else
-#  define HAVE_STAT_NSEC
 # endif
 #endif
 
