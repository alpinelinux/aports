#!/bin/sh

/sbin/depmod -a

for mod in vboxpci vboxnetadp vboxnetflt vboxdrv; do
	/sbin/modprobe -q ${mod}
done

#cat 1>&2 <<EOF
#*
#* If you want to run VirtualBox right away, without rebooting, then
#* be sure to load the required kernel modules. NOTE: The kernel
#* modules will automatically load at system boot.
#*
#* To load the kernel modules right away:
#*
#* sudo /etc/init.d/modules restart
#*
#EOF

exit 0
