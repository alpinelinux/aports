# Maintainer: psykose <alice@ayaya.dev>
pkgname=cargo-deny
pkgver=0.13.6
pkgrel=0
pkgdesc="Cargo plugin for linting dependencies"
url="https://github.com/EmbarkStudios/cargo-deny"
arch="all"
license="Apache-2.0 OR MIT"
makedepends="
	cargo
	libgit2-dev
	libssh2-dev
	openssl-dev
	zlib-dev
	zstd-dev
	"
source="$pkgname-$pkgver.tar.gz::https://github.com/EmbarkStudios/cargo-deny/archive/refs/tags/$pkgver.tar.gz"
options="!check" # tests seem to fail inside git

export LIBSSH2_SYS_USE_PKG_CONFIG=1

case "$CARCH" in
riscv64)
	options="$options textrels"
	;;
esac

prepare() {
	default_prepare

	# Rust target triple.
	local target=$(rustc -vV | sed -n 's/host: //p')

	# Build against system-provided libzstd.
	mkdir -p .cargo
	cat >> .cargo/config.toml <<-EOF
		[target.$target]
		zstd = { rustc-link-lib = ["zstd"] }
	EOF

	cargo fetch --locked
}

build() {
	cargo build --release --frozen --no-default-features
}

check() {
	cargo test --frozen --no-default-features
}

package() {
	install -Dm755 target/release/cargo-deny -t "$pkgdir"/usr/bin/
}

sha512sums="
9806482f0b68a342ef32e31cac6d0e4faf7d7ca417387c53bb4f953901148bcee008df0324981e1e4d735693c96851c3de4cf1a9f66e33364efdca4d3aec3676  cargo-deny-0.13.6.tar.gz
"
