# Contributor: Coco Liliace <coco@liliace.dev>
# Maintainer: Coco Liliace <coco@liliace.dev>
pkgname=oppa
pkgver=1.0.1
pkgrel=0
pkgdesc="A preprocessor for OpenAPI files"
url="https://sr.ht/~liliace/oppa/"
arch="all"
license="MPL-2.0"
makedepends="cargo cargo-auditable"
source="$pkgname-$pkgver.tar.gz::https://git.sr.ht/~liliace/oppa/archive/$pkgver.tar.gz"

prepare() {
	default_prepare

	cargo fetch --locked --target="$CTARGET"
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	install -Dm755 -t "$pkgdir"/usr/bin ./target/release/oppa
}

sha512sums="
743808c85b80f81e87cdeee7ce8d63705a7a0f165a2f78027754f3412250428e002fe7be64467236858f19d034038510df90a2fbaf3412962918fbad8c3dce85  oppa-1.0.1.tar.gz
"
