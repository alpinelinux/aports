# Larger UDP socket buffers for quic-go (DoQ / DoH3 performance).
# quic-go requests up to 7 MiB; the Alpine default (~208 KiB) triggers a
# "failed to sufficiently increase receive buffer size" warning at startup.
net.core.rmem_max = 7340032
net.core.wmem_max = 7340032
