--- ./cli.go.orig
+++ ./cli.go
@@ -32,7 +32,7 @@
 	app.Name = "lego"
 	app.Usage = "Let's Encrypt client written in Go"
 
-	version := "0.3.0"
+	version := "0.3.1"
 	if strings.HasPrefix(gittag, "v") {
 		version = gittag
 	}
