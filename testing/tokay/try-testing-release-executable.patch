--- a/src/utils.rs
+++ b/src/utils.rs
@@ -63,7 +63,7 @@ pub(crate) fn testcase(code: &str) {
 
         //let program = env::args().next().unwrap(); // Doens't work with cargo test
         let program = if cfg!(test) {
-            "target/debug/tokay".to_string()
+            "target/release/tokay".to_string()
         } else {
             std::env::current_exe()
                 .unwrap()
