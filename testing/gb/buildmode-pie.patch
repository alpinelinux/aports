--- ./context.go.orig
+++ ./context.go
@@ -146,7 +146,7 @@
 	ctx := Context{
 		Project:   p,
 		workdir:   workdir,
-		buildmode: "exe",
+		buildmode: "pie",
 		pkgs:      make(map[string]*Package),
 	}
 
