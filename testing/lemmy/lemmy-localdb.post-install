#!/bin/sh

echo "--"
echo "Create a PostgreSQL user and database by running /usr/lib/lemmy/db-init.sh."
echo "Edit the config at /etc/lemmy/lemmy.hjson, a lot of optional lines can be deleted."
echo "Run the lemmy-ui service, and configure pict-rs if you have enabled it in the config."
echo "--"
