# Contributor: Hannes Braun <hannes@hannesbraun.net>
# Maintainer: Hannes Braun <hannes@hannesbraun.net>
pkgname=maildir-rank-addr
pkgver=1.4.1
pkgrel=1
pkgdesc="Creates a ranked list of email addresses from maildir folders"
url="https://github.com/ferdinandyb/maildir-rank-addr"
arch="all"
license="MIT"
makedepends="go"
subpackages="$pkgname-doc"
source="https://github.com/ferdinandyb/maildir-rank-addr/archive/v$pkgver/maildir-rank-addr-$pkgver.tar.gz"
options="net"

build() {
	go build
}

check() {
	go test -v ./...
}

package() {
	install -Dm755 maildir-rank-addr "$pkgdir"/usr/bin/maildir-rank-addr
	install -Dm644 LICENSE -t "$pkgdir"/usr/share/licenses/$pkgname/
}

sha512sums="
ac235fca38b9092d2f07c09f8f90f8a62632ea95831b2c98d44f99bd65e7817a872d5e24519dd85d147c61b594d1c5ad8ff28c06b378b5e885366277f79f4294  maildir-rank-addr-1.4.1.tar.gz
"
