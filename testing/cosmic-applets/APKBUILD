# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-applets
pkgver=1.0.0_beta1
_upstreamver="${pkgver/_beta/-beta.}"
pkgrel=0
pkgdesc="Applets for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-applets"
arch="aarch64 x86_64"
license="GPL-3.0-only"
depends="
	cosmic-icons
"
makedepends="
	cargo
	clang-libclang
	dbus-dev
	findutils
	just
	libinput-dev
	libxkbcommon-dev
	pipewire-dev
	pulseaudio-dev
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-applets/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	just build-release --frozen
}

check() {
	cargo test --release --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
7d90d156e4a0240cf3423a287d1ee45f4d062c69e0c79d5681f727915efd83eeaafa6968ccda2e25f594b68105c7b21b2d39230471a6ec8ea527f754840da152  cosmic-applets-1.0.0_beta1.tar.gz
"
