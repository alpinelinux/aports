--- a/pkg/passive/sources_wo_auth_test.go
+++ b/pkg/passive/sources_wo_auth_test.go
@@ -38,6 +38,8 @@
 		"leakix",         // now requires API key (returns 401)
 		"reconeer",       // now requires API key (returns 401)
 		"sitedossier",    // flaky - returns no results in CI
+		"rapiddns",       // failing in CI/CD due to Cloudflare "protection"
+		"submd",          // failing in CI/CD due to Cloudflare "protection"
 	}
 
 	domain := "hackerone.com"
