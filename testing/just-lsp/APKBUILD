# Contributor: Marian Buschsieweke <marian.buschsieweke@posteo.net>
maintainer="Marian Buschsieweke <marian.buschsieweke@posteo.net>"
pkgname=just-lsp
pkgver=0.5.0
pkgrel=0
pkgdesc="Language server for just"
url="https://github.com/terror/just-lsp"
arch="all"
license="CC0-1.0"
makedepends="
	cargo-auditable
	"
source="$pkgname-$pkgver.tar.gz::https://github.com/terror/just-lsp/archive/refs/tags/$pkgver.tar.gz"

export CARGO_PROFILE_RELEASE_OPT_LEVEL="z"

prepare() {
	default_prepare

	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	install -D -m755 target/release/just-lsp -t "$pkgdir"/usr/bin/
}

sha512sums="
1c09984df166f22354ff86e26e710ddaa924871c954916228e177eaa6823590622c44d806dbe056b0b8f5d275e2e05aeeebce8378f0e80aff1492b041b09713e  just-lsp-0.5.0.tar.gz
"
