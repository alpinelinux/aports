# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-osd
pkgver=1.0.0_beta3
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="On-Screen Display for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-osd"
# 32-bit: https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/issues/111
# s390x: nix
arch="all !armhf !armv7 !x86 !s390x"
license="GPL-3.0-only"
makedepends="
	cargo
	clang-libclang
	eudev-dev
	just
	libinput-dev
	libxkbcommon-dev
	pipewire-dev
	pulseaudio-dev
	wayland-dev
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-osd/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	make polkit-agent-helper-1=/usr/lib/polkit-1/polkit-agent-helper-1 \
		ARGS+=" --frozen --release"
}

check() {
	cargo test --release --frozen
}

package() {
	make DESTDIR="$pkgdir" install
}

sha512sums="
090125fe2f664ed1993a57860eb314674da3ab8c282aee3bb307faa05e672b18d9571a2cb26c65a478a0a95a142de82d9cd4de41440b8d0ec4f85228a8bc3148  cosmic-osd-1.0.0_beta3.tar.gz
"
