--- a/Makefile
+++ b/Makefile
@@ -1,5 +1,6 @@
 VERSION ?= 0.7
-CFLAGS ?= -Wall -W -g -O2 -D'IFUPDOWN_VERSION="$(VERSION)"'
+CFLAGS ?= -Wall -W -g -O2 
+CFLAGS += -D'IFUPDOWN_VERSION="$(VERSION)"'
 ARCH ?= linux
 
 BASEDIR ?= $(DESTDIR)
