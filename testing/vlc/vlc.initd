#!/sbin/runscript

depend() {
	need net
}

start() {
	ebegin "Starting VLC"
	start-stop-daemon --start \
		--user vlc \
		--exec /usr/bin/vlc -- ${VLC_OPTS}
	eend $?
}

stop () {
	ebegin "Stopping VLC"
	start-stop-daemon --stop \
		--signal KILL \
		--exec /usr/bin/vlc
	eend $?
}

