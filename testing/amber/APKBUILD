# Contributor: Guy Godfroy <guy.godfroy@gugod.fr>
# Maintainer: Guy Godfroy <guy.godfroy@gugod.fr>
pkgname=amber
pkgver=0.6.0_alpha
pkgrel=0
pkgdesc="Programming language compiled to bash"
url="https://amber-lang.com"
arch="all"
license="GPL-3.0-or-later"
depends="bc bash coreutils"
makedepends="cargo cargo-auditable"
source="$pkgname-$pkgver.tar.gz::https://github.com/amber-lang/amber/archive/refs/tags/${pkgver/_/-}.tar.gz"
builddir="$srcdir/$pkgname-${pkgver/_/-}"
options="net"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo auditable build --frozen --release
}

check() {
	# bash_error_exit_code expect debug build
	# the other tests are broken and are supposed to be fixed in 0.5.0
	# download and http_fetch need network
	cargo test --frozen -- \
		--skip bash_error_exit_code \
		--skip download \
		--skip http_fetch \
		--skip test_stdlib_src_tests_stdlib_text_replace_regex_ext_ab
}

package() {
	install -Dm755 target/release/amber -t "$pkgdir"/usr/bin
}

sha512sums="
b4bfef64143bb79f975d0d2ce9468ba5b45e04d3d7704e0b0a1ff97c7b5a0013082635e4d398a86726a74087081d1adfbeaa9e106f3702aa7b59dcf46b4f5e95  amber-0.6.0_alpha.tar.gz
"
