#!/bin/sh

printf "\nYou need to add pg_cron to shared_preload_libraries in postgresql.conf and restart PostgreSQL.\n\
Note that pg_cron does not run any jobs as a long a server is in hot standby mode,\n\
but it automatically starts when the server is promoted.\n\n"

printf "# add to postgresql.conf:\nshared_preload_libraries = 'pg_cron'\n\n"

