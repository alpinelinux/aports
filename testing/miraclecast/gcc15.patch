--- a/src/shared/wpas.c
+++ b/src/shared/wpas.c
@@ -1315,7 +1315,7 @@ static int wpas__send(struct wpas *w, struct wpas_message *m)
 		   m->raw,
 		   m->rawlen,
 		   MSG_NOSIGNAL,
-		   w->server ? &m->peer : NULL,
+		   w->server ? (struct sockaddr *)&m->peer : NULL,
 		   w->server ? sizeof(m->peer) : 0);
 	if (l < 0) {
 		if (errno == EAGAIN || errno == EINTR)
