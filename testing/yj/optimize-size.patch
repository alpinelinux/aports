Decrease binary size from 1.2 MiB to 0.7 MiB.

--- a/Cargo.toml
+++ b/Cargo.toml
@@ -24,3 +24,9 @@
 [dependencies.structopt]
 features = ["wrap_help"]
 version = "0.3.12"
+
+[profile.release]
+codegen-units = 1
+lto = true
+opt-level = 'z'
+panic ='abort'
