# Maintainer: Florian Mirkes <fmirkes@mailbox.org>
pkgname=hister
pkgver=0.18.0
pkgrel=0
pkgdesc="Your own search engine"
url="https://hister.org"
arch="aarch64 x86_64" # other archs are unsupported by upstream
license="AGPL-3.0-or-later"
makedepends="go npm tzdata"
pkgusers="$pkgname"
pkggroups="$pkgname"
install="$pkgname.pre-install"
subpackages="$pkgname-openrc"
options="net"
source="$pkgname-$pkgver.tar.gz::https://github.com/asciimoo/hister/archive/refs/tags/v$pkgver.tar.gz
	$pkgname.initd
	$pkgname.confd
	$pkgname.yml
	"

build() {
	go generate
	go build
}

check() {
	go test ./...
}

package() {
	install -Dm755 "$builddir/$pkgname" "$pkgdir/usr/bin/$pkgname"

	install -Dm755 "$srcdir/$pkgname.initd" "$pkgdir/etc/init.d/$pkgname"
	install -Dm644 "$srcdir/$pkgname.confd" "$pkgdir/etc/conf.d/$pkgname"

	install -Dm640 -o $pkgusers -g $pkggroups "$srcdir/$pkgname.yml" "$pkgdir/etc/$pkgname.yml"
	install -dm750 -o $pkgusers -g $pkggroups "$pkgdir/var/lib/$pkgname"
}

sha512sums="
156baec6b3a5798ea583d5fd7041f47dfd6da83d1c2a189cd60fac8235af82caedd107f42164acf3b3be62b6687668905c1f01788457d0746eb5499044ca1a09  hister-0.18.0.tar.gz
30e02b66fa530cd719c8c896b3c23df4b618d784f96d790fbdf4169edd28a96c7c01f21098e94d3507b1fc3566a5581417a8d1c64a50ad508cd4039c06b52a9b  hister.initd
216b8ea2d668a8822a438109b05aebba3c07a5695833118bc807bb7dcca08064d85ca33b0e9aaeb565e6343b76ae67da01b9944f250e2c904cb9345369f89ad0  hister.confd
84e85d326f969eafc0680e9d31737901431f13ef60c8241a03f78076bc66da17da23f5acff86d8ad3737b3ae9c8b8fbe5113d6509b854678b558860a0a3f2e9e  hister.yml
"
