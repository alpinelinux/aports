# Configuration for /etc/init.d/mtail

# User (and group) to run mtail as.
#command_user=mtail:mtail

ARGS="--progs /etc/mtail --logs /var/log/messages"

# Comment out to disable process supervisor.
supervisor=supervise-daemon
