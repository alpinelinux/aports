submitted upstream as https://github.com/adferrand/dnsrobocert/pull/306

--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,8 +1,6 @@
 [build-system]
-requires = [
-    "poetry>=0.12"
-]
-build-backend = "poetry.masonry.api"
+requires = ["poetry-core"]
+build-backend = "poetry.core.masonry.api"
 
 [tool.poetry]
 name = "dnsrobocert"
