From 7e04f61c68329d60662429cd0f386fcda33f9ee1 Mon Sep 17 00:00:00 2001
From: Bart Ribbers <bribbers@disroot.org>
Date: Fri, 20 Mar 2026 16:10:56 +0100
Subject: [PATCH 3/3] Port to Qt6

---
 src/aboutsettings.cpp    | 5 +++--
 src/certificatemodel.cpp | 2 +-
 src/datetimesettings.cpp | 8 ++++----
 src/deviceinfo.cpp       | 4 ++--
 src/languagemodel.cpp    | 1 -
 src/partitionmanager.cpp | 4 ++--
 src/plugin/plugin.pro    | 2 +-
 src/src.pro              | 8 ++++----
 src/udisks2block.cpp     | 3 ++-
 9 files changed, 19 insertions(+), 18 deletions(-)

diff --git a/src/aboutsettings.cpp b/src/aboutsettings.cpp
index d3c60f0..aae8bd0 100644
--- a/src/aboutsettings.cpp
+++ b/src/aboutsettings.cpp
@@ -43,6 +43,8 @@
 #include <QVariant>
 #include <QSettings>
 #include <QTimer>
+#include <QRegExp>
+#include <QLocale>
 
 namespace
 {
@@ -62,7 +64,7 @@ void parseReleaseFile(const QString &filename, QMap<QString, QString> *result)
 
         // "All strings should be in UTF-8 format, and non-printable characters
         // should not be used."
-        in.setCodec("UTF-8");
+        in.setEncoding(QStringConverter::Utf8);
 
         while (!in.atEnd()) {
             QString line = in.readLine();
@@ -127,7 +129,6 @@ void parseLocalizationFile(const QString &filename, QMap<QString, QString> *resu
     }
 
     QSettings localizations(filename, QSettings::IniFormat);
-    localizations.setIniCodec("UTF-8");
 
     QStringList languages = QLocale::system().uiLanguages();
     QStringList availableLanguages;
diff --git a/src/certificatemodel.cpp b/src/certificatemodel.cpp
index 0686721..a83f1ea 100644
--- a/src/certificatemodel.cpp
+++ b/src/certificatemodel.cpp
@@ -318,7 +318,7 @@ private:
 
         // Join any data with the preceding header
         data.replace(QRegularExpression(": *\n +"), QStringLiteral(":"));
-        foreach (const QString &line, data.split(QString("\n"), QString::SkipEmptyParts)) {
+        foreach (const QString &line, data.split(QString("\n"), Qt::SkipEmptyParts)) {
             int index = line.indexOf(QChar(':'));
             if (index != -1) {
                 QString name(line.left(index));
diff --git a/src/datetimesettings.cpp b/src/datetimesettings.cpp
index 2a59415..a5345e6 100644
--- a/src/datetimesettings.cpp
+++ b/src/datetimesettings.cpp
@@ -31,8 +31,8 @@
 
 #include "datetimesettings.h"
 
-#include <timed-qt5/interface>
-#include <timed-qt5/wallclock>
+#include <timed-qt6/interface>
+#include <timed-qt6/wallclock>
 #include <QDebug>
 
 class DateTimeSettingsPrivate: public QObject
@@ -191,14 +191,14 @@ void DateTimeSettings::setTime(int hour, int minute)
     QDate currentDate = QDate::currentDate();
     QTime time(hour, minute);
     QDateTime newTime(currentDate, time);
-    d_ptr->setTime(newTime.toTime_t());
+    d_ptr->setTime(newTime.toSecsSinceEpoch());
 }
 
 void DateTimeSettings::setDate(const QDate &date)
 {
     QDateTime newTime = QDateTime::currentDateTime();
     newTime.setDate(date);
-    d_ptr->setTime(newTime.toTime_t());
+    d_ptr->setTime(newTime.toSecsSinceEpoch());
 }
 
 bool DateTimeSettings::automaticTimeUpdate()
diff --git a/src/deviceinfo.cpp b/src/deviceinfo.cpp
index 880521b..106e50a 100644
--- a/src/deviceinfo.cpp
+++ b/src/deviceinfo.cpp
@@ -205,8 +205,8 @@ void DeviceInfoPrivate::modemAdded(const QString &modemName)
 
 void DeviceInfoPrivate::modemsChanged(const QStringList &modems)
 {
-    QSet<QString> previous(m_modemList.toSet());
-    QSet<QString> current(modems.toSet());
+    QSet<QString> previous(m_modemList.begin(), m_modemList.end());
+    QSet<QString> current(modems.begin(), modems.end());
     QSet<QString> added(current - previous);
     QSet<QString> removed(previous - current);
     for (auto iter = removed.cbegin(); iter != removed.cend(); ++iter)
diff --git a/src/languagemodel.cpp b/src/languagemodel.cpp
index febd34d..17ad504 100644
--- a/src/languagemodel.cpp
+++ b/src/languagemodel.cpp
@@ -209,7 +209,6 @@ QList<Language> LanguageModel::supportedLanguages()
 
     foreach (const QFileInfo &fileInfo, fileInfoList) {
         QSettings settings(fileInfo.filePath(), QSettings::IniFormat);
-        settings.setIniCodec("UTF-8");
         QString name = settings.value("Name").toString();
         QString localeCode = settings.value("LocaleCode").toString();
         QString region = settings.value("Region").toString();
diff --git a/src/partitionmanager.cpp b/src/partitionmanager.cpp
index 3ab8e0a..22fce96 100644
--- a/src/partitionmanager.cpp
+++ b/src/partitionmanager.cpp
@@ -90,8 +90,8 @@ public:
                                           0LL);
             }
 
-            struct statvfs64 stat;
-            if (::statvfs64(partition->mountPath.toUtf8().constData(), &stat) == 0) {
+            struct statvfs stat;
+            if (::statvfs(partition->mountPath.toUtf8().constData(), &stat) == 0) {
                 qint64 bytesFree = stat.f_bfree * stat.f_frsize;
                 qint64 bytesAvailable = std::min((qint64)(stat.f_bavail * stat.f_frsize), quotaAvailable);
 
diff --git a/src/plugin/plugin.pro b/src/plugin/plugin.pro
index ecfdf5e..dcac268 100644
--- a/src/plugin/plugin.pro
+++ b/src/plugin/plugin.pro
@@ -6,7 +6,7 @@ CONFIG += qt plugin hide_symbols link_pkgconfig
 QT += qml dbus network
 QT -= gui
 
-PKGCONFIG += profile usb-moded-qt5 libsailfishkeyprovider connman-qt5
+PKGCONFIG += profile usb-moded-qt6 libsailfishkeyprovider connman-qt6
 
 target.path = $$[QT_INSTALL_QML]/$$PLUGIN_IMPORT_PATH
 INSTALLS += target
diff --git a/src/src.pro b/src/src.pro
index 9be5162..c45d525 100644
--- a/src/src.pro
+++ b/src/src.pro
@@ -2,13 +2,13 @@ TEMPLATE = lib
 TARGET = systemsettings
 
 CONFIG += qt create_pc create_prl no_install_prl
-QT += dbus network
+QT += dbus network core5compat
 QT -= gui
 
 CONFIG += hide_symbols link_pkgconfig
-PKGCONFIG += profile mlite5 mce timed-qt5 blkid libcrypto libsailfishkeyprovider connman-qt5 glib-2.0
+PKGCONFIG += profile mlite6 mce timed-qt6 blkid libcrypto libsailfishkeyprovider connman-qt6 glib-2.0
 PKGCONFIG += ssu-sysinfo nemodbus libsystemd sailfishusermanager sailfishaccesscontrol
-PKGCONFIG += qofono-qt5
+PKGCONFIG += qofono-qt6
 
 system($$[QT_INSTALL_BINS]/qdbusxml2cpp -p mceiface.h:mceiface.cpp mce.xml)
 
@@ -100,6 +100,6 @@ QMAKE_PKGCONFIG_DESCRIPTION = System settings application development files
 QMAKE_PKGCONFIG_LIBDIR = $$target.path
 QMAKE_PKGCONFIG_INCDIR = $$develheaders.path
 QMAKE_PKGCONFIG_DESTDIR = pkgconfig
-QMAKE_PKGCONFIG_REQUIRES = Qt5Core Qt5DBus
+QMAKE_PKGCONFIG_REQUIRES = Qt6Core Qt6DBus
 
 INSTALLS += target develheaders pkgconfig locationconfig compat_locationconfig
diff --git a/src/udisks2block.cpp b/src/udisks2block.cpp
index 4df6726..07aa6d0 100644
--- a/src/udisks2block.cpp
+++ b/src/udisks2block.cpp
@@ -2,6 +2,7 @@
 #include "udisks2defines.h"
 #include "logging_p.h"
 
+#include <QSequentialIterable>
 #include <nemo-dbus/dbus.h>
 #include <nemo-dbus/interface.h>
 #include <nemo-dbus/connection.h>
@@ -351,7 +352,7 @@ QStringList UDisks2::Block::symlinks() const
             if (a.canConvert<QVariantList>()) {
                 QSequentialIterable i = a.value<QSequentialIterable>();
                 for (const QVariant &variantByte : i) {
-                    symlinkBytes.append(variantByte.toChar());
+                    symlinkBytes.append(variantByte.toByteArray());
                 }
             }
 
-- 
2.53.0

