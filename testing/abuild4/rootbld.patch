Otherwise one would need to set EXTRADEPENDS_HOST="-t abuild abuild4"
manually but still needs $mirror/$version/testing in .rootbld-repositories

--- a/abuild.in
+++ b/abuild.in
@@ -2596,7 +2596,7 @@ rootbld() {
 	$ROOTBLD_APK add "$@" --initdb --update \
 		--arch $CBUILD_ARCH \
 		--root "$BUILD_ROOT" \
-		abuild alpine-base $hostdeps $builddeps
+		--virtual abuild abuild4 alpine-base $hostdeps $builddeps
 
 	# busybox adduser/addgroup check if uid is 0 so we need to generate the files manually
 	awk -F: -v pkggroups="$pkggroups" -v pkgusers="$pkgusers" -v root="$BUILD_ROOT" 'BEGIN {
