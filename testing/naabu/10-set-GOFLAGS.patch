--- a/Makefile
+++ b/Makefile
@@ -3,7 +3,7 @@
 GOBUILD=$(GOCMD) build
 GOMOD=$(GOCMD) mod
 GOTEST=$(GOCMD) test
-GOFLAGS := -v 
+GOFLAGS := $(GOFLAGS) -v 
 LDFLAGS := -s -w
 
 ifneq ($(shell go env GOOS),darwin)
