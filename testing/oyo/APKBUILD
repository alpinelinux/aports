# Maintainer: omni <omni+alpine@hack.org>
maintainer="omni <omni+alpine@hack.org>"
pkgname=oyo
pkgver=0.1.35
pkgrel=0
pkgdesc="diff & review TUI"
url="https://github.com/ahkohd/oyo"
license="MIT"
arch="all"
makedepends="cargo cargo-auditable"
subpackages="$pkgname-doc"
source="$pkgname-$pkgver.tar.gz::https://github.com/ahkohd/oyo/archive/refs/tags/v$pkgver.tar.gz"

prepare() {
	default_prepare

	cargo fetch --target="$CHOST" --locked
}

build() {
	cargo auditable build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	install -Dm755 -t "$pkgdir"/usr/bin/ target/release/oy
	install -Dm644 -t "$pkgdir"/usr/share/doc/"$pkgname"/ \
		README.md docs/*
	install -Dm644 -t "$pkgdir"/usr/share/licenses/"$pkgname"/ \
		LICENSE.md
}

sha512sums="
457dd8de3d9114712dd2fae4c91ff81ac22627711585ac666a03f5be64b5c661dc4f06fd3a0b4b9421141cd3f9639e2cf337e47c461be93ef846590ffe3c7768  oyo-0.1.35.tar.gz
"
