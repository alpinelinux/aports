#!/bin/sh


case $1 in
	pre_install)
		adduser -H -h /var/www/localhost/htdocs -s /bin/false -D \
			lighttpd 2>/dev/null
		;;
	post_install)
		chown lighttpd:lighttp /var/*/lighttpd
		;;
esac

exit 0
