--- a/src/gfanlib_z.h	2026-06-01 14:33:46.108489969 -0400
+++ b/src/gfanlib_z.h	2026-06-01 14:33:46.112581099 -0400
@@ -14,6 +14,7 @@
 #define OLD 1
 #if OLD
 #include "gmp.h"
+#include <cstdint>
 
 namespace gfan{
 
--- a/src/packedmonomial.h	2026-06-01 14:33:46.109715322 -0400
+++ b/src/packedmonomial.h	2026-06-01 14:33:46.114715291 -0400
@@ -10,6 +10,7 @@
 
 #include <bitset>
 #include <iostream>
+#include <cstdint>
 #include "monomial.h"
 #include "printer.h"
 
@@ -29,7 +30,7 @@
 	struct VariableData
 	{
 //		int64 mask;
-		u_int64_t mask;
+		uint64_t mask;
 		int index;
 		int shift;
 		VariableData(int64 mask_, int index_, int shift_):
@@ -44,7 +45,7 @@
 	struct WordData
 	{
 //		int64 overflowMasks;
-		u_int64_t overflowMasks;
+		uint64_t overflowMasks;
 		int freeBits;
 		WordData():
 			overflowMasks(0),
@@ -143,12 +144,12 @@
 template<int NWORDS>
 class PackedMonomial{
 public:
-	u_int64_t data[NWORDS];
+	uint64_t data[NWORDS];
 //	int64 data[NWORDS];
 public:
-	u_int16_t hash()const
+	uint16_t hash()const
 	{
-		u_int64_t s=0;
+		uint64_t s=0;
 		for(int i=0;i<NWORDS;i++)
 			s+=data[i];
 //		std::cerr<<"S:"<<s<<" "<< (((0xa847c7b39b4369aeull*s)>>48) &0xffffull)<<"\n";
@@ -170,7 +171,7 @@
 		for(int i=0;i<nExps;i++)
 			data[man.variableData[i].index]|=(exponent[i]&man.variableData[i].mask)<<man.variableData[i].shift;
 	}
-	u_int64_t extractWeight(PacMan const &man)const
+	uint64_t extractWeight(PacMan const &man)const
 	{
 		return data[man.variableData.back().index]>>man.variableData.back().shift&man.variableData.back().mask;
 	}
--- a/src/gfanlib_circuittableint.h	2026-06-01 14:33:46.111193828 -0400
+++ b/src/gfanlib_circuittableint.h	2026-06-01 14:33:46.116528373 -0400
@@ -64,28 +64,28 @@
 	return s.str();
 }
 
-static std::string toStr(__int64_t b)
+static std::string toStr(int64_t b)
 {
 	std::stringstream s;
 	s<<b;
 	return s.str();
 }
 
-static std::string toStr(__uint64_t b)
+static std::string toStr(uint64_t b)
 {
 	std::stringstream s;
 	s<<b;
 	return s.str();
 }
 
-static std::string toStr(__int32_t b)
+static std::string toStr(int32_t b)
 {
 	std::stringstream s;
 	s<<b;
 	return s.str();
 }
 
-static std::string toStr(__uint32_t b)
+static std::string toStr(uint32_t b)
 {
 	std::stringstream s;
 	s<<b;
