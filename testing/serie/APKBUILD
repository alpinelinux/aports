# Maintainer: qaqland <qaq@qaq.land>
maintainer="qaqland <qaq@qaq.land>"
pkgname=serie
pkgver=0.5.1
pkgrel=0
pkgdesc="A rich git commit graph in your terminal"
url="https://github.com/lusingander/serie"
arch="all !s390x" # test failed
license="MIT"
depends="git"
makedepends="cargo cargo-auditable"
subpackages="$pkgname-doc"
source="https://github.com/lusingander/serie/archive/v$pkgver/serie-$pkgver.tar.gz"
options="net" # cargo fetch

prepare() {
	default_prepare

	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo auditable build --frozen --release
}

check() {
	cargo test --frozen
}

package() {
	install -Dm755 target/release/$pkgname -t "$pkgdir"/usr/bin
	install -Dm644 README.md -t "$pkgdir"/usr/share/doc/$pkgname
	install -Dm644 LICENSE -t "$pkgdir"/usr/share/licenses/$pkgname
}

sha512sums="
c0666779fb15a03529e379d09eed89acf2518acb1009e7926922307261a08819bd67ec2e6ec65cdbf462e1daa3288b9000ffc379a527c330d3cad5d82f7a0c47  serie-0.5.1.tar.gz
"
