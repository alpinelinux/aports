# Contributor: Clayton Craft <clayton@craftyguy.net>
# Maintainer: Clayton Craft <clayton@craftyguy.net>
pkgname=cosmic-initial-setup
pkgver=1.0.0_beta6
_upstreamver="${pkgver/_beta/-beta.}"
_upstreamver="${_upstreamver/_p/.}"
pkgrel=0
pkgdesc="Initial setup for the COSMIC Desktop Environment"
url="https://github.com/pop-os/cosmic-initial-setup"
arch="all"
license="GPL-3.0-or-later"
depends="
	cosmic-icons
"
makedepends="
	cargo
	eudev-dev
	desktop-file-utils
	just
	libinput-dev
	libxkbcommon-dev
	mesa-dev
	openssl-dev
	wayland-dev
	wayland-protocols
"
options="net !check"  # fetch dependencies, no tests
source="$pkgname-$pkgver.tar.gz::https://github.com/pop-os/cosmic-initial-setup/archive/refs/tags/epoch-$_upstreamver.tar.gz"
builddir="$srcdir/$pkgname-epoch-$_upstreamver"

prepare() {
	default_prepare
	cargo fetch --target="$CTARGET" --locked
}

build() {
	cargo build --release --frozen
}

check() {
	cargo test --release --frozen
}

package() {
	just rootdir="$pkgdir" install
}

sha512sums="
40955a1de49dc39003a9ff18b322c7d7db6d675a98c6ea28e66524a39e106307ec34cc6d3504405734d4bf2b3ffc160ade4947559fd34406d26636a02422259d  cosmic-initial-setup-1.0.0_beta6.tar.gz
"
