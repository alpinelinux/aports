# Contributor: Jakob Meier <comcloudway@ccw.icu>
# Maintainer: Jakob Meier <comcloudway@ccw.icu>
pkgname=g4music
pkgver=3.9
pkgrel=0
pkgdesc="Fast fluent lightweight music player written in GTK4, with a beautiful and adaptive user interface"
url="https://gitlab.gnome.org/neithern/g4music"
arch="all"
license="GPL-3.0-only"
subpackages="$pkgname-lang"
makedepends="
	cmake
	desktop-file-utils
	gettext-dev
	gst-plugins-base-dev
	gstreamer-dev
	libadwaita-dev
	meson
	vala
	"
source="https://gitlab.gnome.org/neithern/g4music/-/archive/v$pkgver/g4music-v$pkgver.tar.gz"
builddir="$srcdir/g4music-v$pkgver"

build() {
	abuild-meson \
		. output
	meson compile -C output
}

check() {
	meson test --no-rebuild --print-errorlogs -C output
}

package() {
	DESTDIR="$pkgdir" meson install --no-rebuild -C output
}

sha512sums="
f7277f979bf0254a0f823f0ef99e3e36852017601caac6aaa19190f1c488d8ac8a0ff7cbefb1b905426aafad57507a9ee378c0919e3d1ddf94f813638500bf54  g4music-v3.9.tar.gz
"
