diff --git a/test/util/chamuyero b/test/util/chamuyero
index 0703501..d22ee26 100755
--- a/test/util/chamuyero
+++ b/test/util/chamuyero
@@ -74,8 +74,16 @@ class Sock(object):
 
     def write(self, s):
         self.has_conn.wait()
-        self.connw.write(s)
-        self.connw.flush()
+        try:
+            self.connw.write(s)
+            self.connw.flush()
+        except (BrokenPipeError, ConnectionResetError):
+            # The peer may close the connection in the middle of the dialog,
+            # e.g. when rejecting an SMTP smuggling attempt. On slow systems
+            # that close can arrive before we finish writing. The expected
+            # response is validated by the following read, so it is safe to
+            # ignore the write failure here.
+            pass
 
     def readline(self):
         self.has_conn.wait()
