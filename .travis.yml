sudo: required
install:
  - sudo .travis/install-alpine
  - sudo .travis/setup-alpine
script: |
  source .travis/common.sh
  alpine_run $ALPINE_USER .travis/build-pkgs
after_success:
  - cd scripts && ./mkimage.sh --platform rpi --arch armhf
notifications:
  email: false
