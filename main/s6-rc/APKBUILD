# Maintainer: Laurent Bercot <ska-devel@skarnet.org>
# Contributor: Eivind Uggedal <eu@eju.no>
pkgname=s6-rc
pkgver=0.5.2.3
pkgrel=0
pkgdesc="Service manager for s6-based systems"
url="http://skarnet.org/software/$pkgname/"
arch="all"
license="ISC"
options="!check"
makedepends="skalibs-dev>=2.11 execline-dev s6-dev>=2.11"
subpackages="$pkgname-static $pkgname-dev $pkgname-doc"
source="https://skarnet.org/software/s6-rc/s6-rc-$pkgver.tar.gz"

build() {
	./configure \
		--enable-shared \
		--enable-static \
		--disable-allstatic \
		--libdir=/usr/lib \
		--libexecdir="/lib/$pkgname" \
		--with-dynlib=/lib
	make
}

package() {
	make DESTDIR="$pkgdir" install
}

doc() {
	default_doc
	mkdir -p "$subpkgdir/usr/share/doc"
	cp -a "$builddir/doc" "$subpkgdir/usr/share/doc/$pkgname"
}

static() {
	default_static
	rmdir -p "$pkgdir"/usr/lib || :
}

sha512sums="
9b8f32fbae1ee5ab319ee455ca12f65a1fe37f2d152a43cf0bcdd0a1b1b67ea2f52b1b3f6dcb3935aa09e1a85e6fee7946fe09b389f1799ae80103c0938a38b7  s6-rc-0.5.2.3.tar.gz
"
