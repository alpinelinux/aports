# Contributor: Jakub Jirutka <jakub@jirutka.cz>
# Maintainer: Jakub Jirutka <jakub@jirutka.cz>
pkgname=ruby-net-imap
_gemname=net-imap
# Keep version in sync with "Bundled gems" (https://stdgems.org) for the
# packaged Ruby version.
pkgver=0.5.15
pkgrel=0
pkgdesc="Ruby client api for Internet Message Access Protocol"
url="https://github.com/ruby/net-imap"
arch="noarch"
license="BSD-2-Clause AND Ruby"
depends="ruby"
makedepends="ruby-rdoc"
checkdepends="
	ruby-base64
	ruby-rake
	ruby-test-unit
	ruby-test-unit-ruby-core
	"
subpackages="$pkgname-doc"
source="https://github.com/ruby/net-imap/archive/v$pkgver/$_gemname-$pkgver.tar.gz
	gemspec.patch
	"
builddir="$srcdir/$_gemname-$pkgver"

# secfixes:
#   0.5.15-r0:
#     - CVE-2026-47240
#     - CVE-2026-47241
#     - CVE-2026-47242

prepare() {
	default_prepare
	sed -i '/require .bundler/d' Rakefile
}

build() {
	gem build $_gemname.gemspec
}

check() {
	SIMPLECOV_DISABLE=1 rake test TESTOPTS="--verbose"
}

package() {
	local gemdir="$pkgdir/$(ruby -e 'puts Gem.default_dir')"

	gem install \
		--local \
		--install-dir "$gemdir" \
		--ignore-dependencies \
		--document ri \
		--verbose \
		$_gemname

	# Remove unnessecary files
	cd "$gemdir"
	rm -rf build_info cache extensions plugins
}

doc() {
	pkgdesc="$pkgdesc (ri docs)"

	amove "$(ruby -e 'puts Gem.default_dir')"/doc
}

sha512sums="
3fec4dd0b7027c4d255f2f08c43e7961911fbdf67c0da2a423dcc94856e36765f007c06753b6f7e95d707f92097643b225d09c88301638c05f39b0eead893b71  net-imap-0.5.15.tar.gz
0e45fc58a667561a0ecf1db75013c9c33132073e945599d015e7f038684920cc49ae920133044c254a0b68dc25a290735c026ecafe1ffd301e9e436a3eda5d50  gemspec.patch
"
