#!/bin/sh

addgroup -S aria2 2>/dev/null
adduser -S -D -H -h /var/lib/aria2 -s /sbin/nologin -G aria2 \
	-g 'added by apk for aria2-daemon' aria2 2>/dev/null

su - aria2 -s /bin/sh -c "
	touch ~/aria2.session
	chmod 750 ~/aria2.session
"

exit 0