# Contributor: Łukasz Jendrysik
# Contributor: Sören Tempel <soeren+alpine@soeren-tempel.net>
# Maintainer: Natanael Copa <ncopa@alpinelinux.org>
pkgname=chrony
pkgver=4.8
pkgrel=4
pkgdesc="NTP client and server programs"
url="https://chrony-project.org/"
arch="all"
license="GPL-2.0-or-later"
makedepends="
	asciidoctor
	libcap-dev
	texinfo
	nettle-dev
	gnutls-dev
	libseccomp-dev
	"
checkdepends="bash"
pkgusers="$pkgname"
pkggroups="$pkgname"
install="$pkgname.pre-install $pkgname.pre-upgrade"
subpackages="
	$pkgname-dbg
	$pkgname-doc
	$pkgname-openrc
	"
source="https://chrony-project.org/releases/chrony-$pkgver.tar.gz
	max_resolve_interval.patch

	chronyd.confd
	chronyd.initd
	chrony.conf
	timepps.h
	"

# secfixes:
#   3.5.1-r0:
#     - CVE-2020-14367

prepare() {
	default_prepare

	# We copy timepps.h to the local build directory instead of
	# creating a pps-tools-dev package for ppstime.h
	# (See https://github.com/ago/pps-tools)
	mkdir -p pps-tools/sys
	cp "$srcdir"/timepps.h pps-tools/sys/
}

build() {
	CPPFLAGS="$CPPFLAGS -I./pps-tools/" \
	CFLAGS="$CFLAGS -flto=auto" \
	./configure \
		--prefix=/usr \
		--mandir=/usr/share/man \
		--sysconfdir=/etc/$pkgname \
		--chronyrundir=/run/chrony \
		--disable-readline \
		--with-ntp-era=0 \
		--with-user="$pkgname" \
		--with-chronyc-user="$pkgname" \
		--with-pidfile=/run/chrony/chronyd.pid \
		--with-sendmail=/usr/sbin/sendmail \
		--enable-ntp-signd \
		--enable-scfilter
	make all docs
}

check() {
	make check
}

package() {
	make DESTDIR="$pkgdir" install

	install -Dm644 examples/chrony.logrotate "$pkgdir"/etc/logrotate.d/chrony
	install -Dm644 examples/*.example -t "$pkgdir"/usr/share/doc/chrony/

	cd "$pkgdir"

	install -d etc/chrony \
		etc/chrony/conf.d \
		etc/chrony/sources.d
	install -Dm644 "$srcdir"/chrony.conf etc/chrony/chrony.conf

	install -Dm644 "$srcdir"/chronyd.confd etc/conf.d/chronyd
	install -Dm755 "$srcdir"/chronyd.initd etc/init.d/chronyd

	install -dm750 -o "$pkgusers" -g "$pkggroups" var/lib/chrony
	install -dm2750 -o "$pkgusers" -g "$pkggroups" var/log/chrony

	install -m640 -o "$pkgusers" -g "$pkggroups" /dev/null var/lib/chrony/chrony.drift
}

sha512sums="
949b796bb34db32a5c1b9e6b53be6a22e51c59f24a316d585b8a52a52ab1f61bdf0378dc58b282bb0ba4fac1f05e1e99fbe37cb4259aa2b359e7bf679c176aab  chrony-4.8.tar.gz
f6f2056d1e70785f66b0fbec6e3ec9b0184785eac262e28e2a2c4d5a49762bf780ed7c2c7990a93f6ef063c167cea01fb8828705be558d0b8ff8c07d6962239f  max_resolve_interval.patch
4e7fe8cd3c2d75b32de3fbf4ffffe2f977c6aa05f902caaaa62dd5d6d7c90b3345bb78c7433585dd5b40ca4fbc662d01730d2f91af327b69e0cfbbfafb84194e  chronyd.confd
f06994481d1ce5346c7d078e04037b7d37c332fb9afdac7bdba3dcf7307d51defc8125bbd7bb622cccf5c536e223d9e044b9b92efaee799057760316dc5af3ad  chronyd.initd
1d6eea554dec8086b7201d41d306d30a4cd92aa0c674fdcbf0cf9e1a5673b247befd90d3544401606da382c389f816170fba5245e00d1e7d0dfeaf6e177ca84c  chrony.conf
172ce4d41d40e0cf22d076eada2bfe6d2b99bb858f3fa80eeba7e677a42bc68fe755cb847d120fff2faa9b07af172a787ec7c4a36005f1df67c19a1bc19825b8  timepps.h
"
