#!/bin/sh

# We need the symlinks early
exec /bin/busybox --install -s

# busybox-pam replaces busybox
# role so manually update world
if [ -n $(ldd /bin/busybox | grep libpam.so) ]; then
        sed -i '/busybox$/d' /etc/apk/world
        sed -i '/busybox>.*=/d' /etc/apk/world
fi

