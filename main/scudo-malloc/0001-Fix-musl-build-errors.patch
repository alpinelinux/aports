--- a/lib/scudo/standalone/tests/wrappers_c_test.cpp
+++ b/lib/scudo/standalone/tests/wrappers_c_test.cpp
@@ -591,7 +591,7 @@ TEST_F(ScudoWrappersCDeathTest, VallocFreeSized) {
 }
 #endif
 
-#if !SCUDO_FUCHSIA
+#if !SCUDO_FUCHSIA && defined(mallopt)
 TEST_F(ScudoWrappersCTest, MallOpt) {
   errno = 0;
   EXPECT_EQ(mallopt(-1000, 1), 0);
@@ -660,7 +660,7 @@ void MallInfoTest() {
 #pragma clang diagnostic pop
 }
 
-#if !SCUDO_FUCHSIA
+#if !SCUDO_FUCHSIA && defined(mallinfo)
 TEST_F(ScudoWrappersCTest, MallInfo) {
 #if SCUDO_ANDROID
   // Android accidentally set the fields to size_t instead of int.
@@ -771,6 +771,7 @@ TEST_F(ScudoWrappersCDeathTest, MallocDisableDeadlock) {
       "");
 }
 
+#ifdef malloc_info
 TEST_F(ScudoWrappersCTest, MallocInfo) {
   // Use volatile so that the allocations don't get optimized away.
   void *volatile P1 = malloc(1234);
@@ -795,6 +796,7 @@ TEST_F(ScudoWrappersCTest, MallocInfo) {
   free(P1);
   free(P2);
 }
+#endif
 
 TEST_F(ScudoWrappersCDeathTest, Fork) {
   void *P;
