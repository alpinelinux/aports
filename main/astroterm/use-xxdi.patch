No more xxd in main/

--- a/scripts/embed.sh
+++ b/scripts/embed.sh
@@ -8,8 +8,8 @@ usage() {
 }
 
 # Ensure xxd is available
-if ! command -v xxd > /dev/null 2>&1; then
-    echo "Error: xxd is not installed or not found in PATH."
+if ! command -v xxdi > /dev/null 2>&1; then
+    echo "Error: xxdi is not installed or not found in PATH."
     exit 1
 fi
 
@@ -46,7 +46,7 @@ if [ -z "$SOURCE" ] || [ -z "$HEADER" ] || [ -z "$ARRAY_NAME" ]; then
 fi
 
 # Generate the header file
-xxd -i "$SOURCE" | \
+xxdi "$SOURCE" | \
     sed "s/unsigned char [^ ]*/unsigned char ${ARRAY_NAME}[]/" | \
     sed "s/unsigned int [^ ]*/unsigned int ${ARRAY_NAME}_len/" > "$HEADER"
 
