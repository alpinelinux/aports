# Options passed to /usr/sbin/syslogd.
# -8 preserves UTF-8 and -m 0 disables MARK messages.  Network policy is
# controlled by secure_mode in /etc/syslog.conf so it can be reloaded.
SYSLOGD_OPTS="-8 -m 0"
