From: Johannes Müller <50362-jogemu@users.gitlab.alpinelinux.org>
Date: Thu, 04 Apr 2026 23:22:56 +0000
Subject: simdjson pad with padded_string

Use padded_string because old version of simdjson does not support pad yet.

--- a/src/node_modules.cc
+++ b/src/node_modules.cc
@@ -104,8 +104,9 @@
   }
   simdjson::ondemand::document document;
   simdjson::ondemand::object main_object;
+  auto padded = simdjson::padded_string(package_config.raw_json);
   simdjson::error_code error =
-      binding_data->json_parser.iterate(simdjson::pad(package_config.raw_json))
+      binding_data->json_parser.iterate(padded)
           .get(document);
 
   const auto throw_invalid_package_config = [error_context, path, realm]() {
--- a/src/node_sea.cc
+++ b/src/node_sea.cc
@@ -402,8 +402,9 @@
   simdjson::ondemand::parser parser;
   simdjson::ondemand::document document;
   simdjson::ondemand::object main_object;
+  auto padded = simdjson::padded_string(config);
   simdjson::error_code error =
-      parser.iterate(simdjson::pad(config)).get(document);
+      parser.iterate(padded).get(document);
 
   if (!error) {
     error = document.get_object().get(main_object);
--- a/src/node_snapshotable.cc
+++ b/src/node_snapshotable.cc
@@ -913,8 +913,9 @@
   simdjson::ondemand::parser parser;
   simdjson::ondemand::document document;
   simdjson::ondemand::object main_object;
+  auto padded = simdjson::padded_string(config_content);
   simdjson::error_code error =
-      parser.iterate(simdjson::pad(config_content)).get(document);
+      parser.iterate(padded).get(document);
 
   if (!error) {
     error = document.get_object().get(main_object);
