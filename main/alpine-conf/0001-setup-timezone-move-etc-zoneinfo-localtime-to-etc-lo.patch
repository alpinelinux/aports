From 41ae5b85c49d3982261b4aab7bf072093670f874 Mon Sep 17 00:00:00 2001
From: Natanael Copa <ncopa@alpinelinux.org>
Date: Wed, 11 Jun 2014 10:17:39 +0200
Subject: [PATCH] setup-timezone: move /etc/zoneinfo/localtime to
 /etc/localtime

This is what upstream musl does.
---
 setup-timezone.in | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/setup-timezone.in b/setup-timezone.in
index 4192465..6964f69 100644
--- a/setup-timezone.in
+++ b/setup-timezone.in
@@ -53,8 +53,8 @@ setup_tz() {
 		cp "$zonepath" "${ROOT}"etc/zoneinfo/$zdir/
 		zonepath=/etc/zoneinfo/$zone
 	fi
-	rm -f "${ROOT}"etc/zoneinfo/localtime
-	ln -s "$zonepath" "${ROOT}"etc/zoneinfo/localtime
+	rm -f "${ROOT}"etc/localtime
+	ln -s "$zonepath" "${ROOT}"etc/localtime
 }
 
 INSTALL_TZDATA=false
-- 
2.0.0

