#%PAM-1.0

# openrc-user will open a PAM session, we need to ensure that
# XDG_RUNTIME_DIR is set in that session through our base-session.
#
# See: https://gitlab.alpinelinux.org/alpine/aports/-/merge_requests/81612#note_492385
session include base-session
