--- ./tools.orig/Makefile
+++ ./tools/Makefile
@@ -198,6 +198,8 @@
 		--extra-ldflags="-L$(XEN_ROOT)/tools/libxc \
 		-L$(XEN_ROOT)/tools/xenstore" \
 		--bindir=$(LIBEXEC) \
+		--libexecdir=$(LIBEXEC) \
+		--sysconfdir=/etc/xen \
 		--datadir=$(SHAREDIR)/qemu-xen \
 		--localstatedir=/var \
 		--disable-kvm \
