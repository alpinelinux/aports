# MySQL (default setup).

production:
  adapter: mysql
  database: redmine
  host: localhost
  username: root
  password:
  encoding: utf8
 
#production:
#  adapter: sqlite3
#  database: redmine
#  dbfile: /var/lib/redmine/redmine.db
 
