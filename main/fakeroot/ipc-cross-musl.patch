Needed when cross compiling fakeroot

--- a/configure.ac
+++ b/configure.ac
@@ -55,7 +55,7 @@ int main() {
 
   if test $ac_cv_use_ipc = cross; then
     case "$host_os" in
-      (linux-gnu*)
+      (linux-gnu*|linux-musl*)
         ac_cv_use_ipc=sysv
         AC_MSG_RESULT([cross, guessing yes])
       ;;
