# Contributor: William Pitcock <nenolod@dereferenced.org>
# Maintainer: William Pitcock <nenolod@dereferenced.org>
pkgname=audacious
pkgver=3.3.4
pkgrel=1
pkgdesc="playlist-oriented media player with multiple interfaces"
url="http://audacious-media-player.org/"
arch="all"
license="ISC"
depends=
depends_dev="gtk+3.0-dev dbus-glib-dev"
makedepends="$depends_dev libguess-dev libxml2-dev autoconf automake"
install=
subpackages="$pkgname-dev $pkgname-lang"
source="http://distfiles.audacious-media-player.org/$pkgname-$pkgver.tar.bz2
	"

_builddir="$srcdir"/$pkgname-$pkgver
prepare() {
	cd "$_builddir"

	for i in $source; do
		case $i in
		*.patch)
			msg "Applying $i..."
			patch -s -p1 -N -i "$srcdir"/$i || return 1
			;;
		esac
	done

	msg "Rebuilding configure..."
	aclocal -I m4 && autoheader && autoconf || return 1
}

build() {
	cd "$_builddir"
	./configure \
		--build=$CBUILD \
		--host=$CHOST \
		--prefix=/usr \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info \
		--enable-chardet \
		|| return 1
	make || return 1
	return 0
}

package() {
	cd "$_builddir"
	make DESTDIR="$pkgdir" install || return 1
}

md5sums="3a622f48009c6d959467b4e819e329ea  audacious-3.3.4.tar.bz2"
sha256sums="671cdbf9b6b722b4ff4f43bcf85da0bdd1dfdf461b510cf6ecdb9187a427fca7  audacious-3.3.4.tar.bz2"
sha512sums="0da20e09dec3104f6e0c16a61eee7fdbd221e21d460544efd1dd2b2f12476f62f0f9042976e6b682510a21fb894fa8a089730ed7a780544e4b49ca6f5b0b6b89  audacious-3.3.4.tar.bz2"
