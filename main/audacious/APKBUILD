# Contributor: William Pitcock <nenolod@dereferenced.org>
# Maintainer: William Pitcock <nenolod@dereferenced.org>
pkgname=audacious
pkgver=3.4.1
pkgrel=0
pkgdesc="A playlist-oriented media player with multiple interfaces"
url="http://audacious-media-player.org/"
arch="all"
license="ISC"
depends=""
depends_dev="gtk+3.0-dev dbus-glib-dev"
makedepends="$depends_dev libguess-dev libxml2-dev autoconf automake"
install=
subpackages="$pkgname-dev $pkgname-lang"
source="http://distfiles.audacious-media-player.org/$pkgname-$pkgver.tar.bz2
	"

_builddir="$srcdir"/$pkgname-$pkgver
prepare() {
	cd "$_builddir"
	update_config_sub || return 1
	for i in $source; do
		case $i in
		*.patch)
			msg "Applying $i..."
			patch -s -p1 -N -i "$srcdir"/$i || return 1
			;;
		esac
	done

	msg "Rebuilding configure..."
	aclocal -I m4 && autoheader && autoconf || return 1
}

build() {
	cd "$_builddir"
	./configure \
		--build=$CBUILD \
		--host=$CHOST \
		--prefix=/usr \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info \
		--enable-chardet \
		|| return 1
	make || return 1
	return 0
}

package() {
	cd "$_builddir"
	make DESTDIR="$pkgdir" install || return 1
}

md5sums="f7f00c2b7d50f2da42be457efe2c376d  audacious-3.4.1.tar.bz2"
sha256sums="57ed4d8378b1e753e266a82424c3ae595b6c0e88fc3e32957720cd6cc14ac971  audacious-3.4.1.tar.bz2"
sha512sums="2a9e203544861a0b902b720823202ef1038927932826847dc9b9a9c321dbecc19e2187afbdeb62a22ee8e6348c7df609ad671c8c63da2abc003a37556df20ac3  audacious-3.4.1.tar.bz2"
