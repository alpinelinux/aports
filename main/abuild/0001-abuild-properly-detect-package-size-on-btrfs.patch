--- abuild.in	2017-01-31 13:03:06.945568903 +0100
+++ abuild.in	2017-01-31 13:02:01.571238791 +0100
@@ -812,7 +812,7 @@
 	cd "$dir"
 	mkdir -p "$controldir"
 	local builddate=$(date -u "+%s")
-	local size=$(sync;du -sk | awk '{print $1 * 1024}')
+	local size=$(du -sk | awk '{print $1 * 1024}')
 
 	if [ "$arch" != "$apkbuild_arch" ]; then
 		local msg="Split function set arch=\"$arch\" for $name, use subpackages=pkg:split:arch format instead"
