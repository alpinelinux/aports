diff --git a/initramfs-init.in b/initramfs-init.in
index 252b9ca..17fcce3 100755
--- a/initramfs-init.in
+++ b/initramfs-init.in
@@ -678,7 +678,7 @@ if [ -n "$KOPT_ssh_key" ]; then
 fi
 
 # use swclock if no RTC is found
-if rtc_exists; then
+if rtc_exists || [ "$(arch)" = "s390x" ]; then
 	rc_add hwclock boot
 else
 	rc_add swclock boot
