#!/bin/sh

cd /usr/share/mime &&
rm -rf globs globs2 icons treemagic generic-icons types magic XMLnamespaces \
	subclasses aliases mime.cache version \
	application audio chemical font image inode message model multipart text \
	video x-content x-epoc
rmdir /usr/share/mime 2>/dev/null

exit 0
