#!/bin/sh

ver_old=$2

if [ "$(apk version -t "$ver_old" '14.0-r4')" = '<' ]; then
	cat >&2 <<-EOF
	*
	* PostgreSQL JIT support has been moved to a subpackage. If you use it, run:
	*   apk add postgresql-jit
	*
	EOF
fi

exit 0
