--- ./lib.nat.orig
+++ ./lib.nat
@@ -683,10 +683,10 @@
 	    fi
 	    ;;
 	!*)
-	    if [ $(list_count $addr) -gt 1 ]; then
+#	    if [ $(list_count $addr) -gt 1 ]; then
 		excludedests="${addr#\!}"
 		addr=
-	    fi
+#	    fi
 	    ;;
     esac
 
