--- ./compiler
+++ ./compiler
@@ -5627,8 +5627,6 @@
 
 if [ -f \$f ]; then
     progress_message2 "Restoring IPSETS..."
-    ipset -U :all: :all:
-    ipset -U :all: :default:
     ipset -F
     ipset -X
     ipset -R < \$f

